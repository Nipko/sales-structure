# Firebase / FCM credentials — secrets, never commit
google-services.json
GoogleService-Info.plist
*-firebase-adminsdk-*.json

# Sentry auth token (source map upload) — secret, never commit
sentry.properties
.sentryclirc

# Expo / React Native
node_modules/
.expo/
dist/
web-build/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
ios/
android/
.env*.local
npm-debug.*
yarn-error.*

# Ejecución del plan de plataforma y agente

Inicio: 6 de septiembre de 2026. Base auditada: `b1e87c71`.

Alcance autorizado: ejecutar íntegramente los planes `assist-agent-experience-audit-2026-09-05.md` y `agent-runtime-learning-plan-2026-09-05.md`, con commits incrementales; reservar decisiones de producto pendientes para el cierre. Este registro conserva requisitos y evidencia sin reducir el alcance a los cambios ya hechos.

Los commits retomados el 7 de septiembre después de `b6db0507`, con su validación por bloque y los frentes pendientes, están registrados en [Reanudación de commits incrementales](audits/2026-09-07/incremental-commits-resumed.md).

**Cierre y traspaso, 8 de septiembre:** por solicitud del usuario se cierra la tanda y se prepara la continuación por Claude. `1f704ddc` registra retención/borrado de respuestas derivadas de Widget; `735a0511`, procedencia acumulada y lectura privada del historial. La integración de salida normal todavía tiene bloqueos y se conserva en un parche verificado, sin activarla. [Traspaso exacto, evidencia y siguiente entrega](handoffs/2026-09-08/claude-execution-handoff.md). Este cierre no declara terminado el plan ni modifica la estimación siguiente.

**Continuación, 8 de septiembre (bloque E3 externo):** outbox durable, transporte estricto y recuperación construidos y validados; ningún productor los usa aún, así que el comportamiento en producción no cambia. [Evidencia y límites](audits/2026-09-08/normal-dispatch-outbox.md).

**Continuación, 8 de septiembre:** el bloqueo del traspaso queda resuelto. `64dc8d75` construye el recibo canónico de handoff ligado al inbound y a la transición exactos y vuelve atómica la transición en todos los canales; `d2ffdf41` integra los seis archivos del parche —verificados contra su manifiesto— con la admisión autorizada por ese recibo en los **siete** puntos de escalada del turno, no los dos que preveía el traspaso; `02dc2e9e` añade la aceptación de historial borrado, carreras durante la admisión y frontera del COMMIT. [Evidencia y límites](audits/2026-09-08/normal-widget-admission-and-handoff-receipt.md). La salida normal **externa** sigue abierta y no hay perfiles certificados nuevos.

Estimación comunicada el 7 de septiembre: **60–65% del plan integral**. Es una valoración de implementación, integración y validación aún pendiente; no una métrica automática por commits, pruebas o requisitos certificados. La base técnica está más avanzada que los pilotos, la cobertura completa por negocio y la medición de calidad. Cero perfiles certificados y ausencia de despliegue siguen siendo límites explícitos.

## Estado por requisito

| ID | Requisito y evidencia de aceptación | Estado |
|---|---|---|
| A1 | Consentimiento contextual, consultas/rechazos sin escritura, propuesta vigente; pruebas multilingües | Consentimiento y términos de citas ligados al comando/cobro y probados con PostgreSQL; términos en otras familias e históricos pendientes |
| A2 | Citas por comando canónico: anticipo, retención, referencia, settlement único y respuesta fiel | Comando, retención, settlement, avisos durables e interfaz de revisión sin reenvío implementados; conciliación de proveedores y piloto pendientes |
| A3 | Borrador antes de reserva, cobro, handoff, media y mensajes externos; aprobación explícita de efectos | Propuestas, consentimiento, aprobación humana y entrega durable, incluido Web Chat, probados con PostgreSQL/Socket.IO; pilotos de proveedores pendientes |
| A4 | Matrículas/cupos y gimnasio atómicos ante errores/reintentos/concurrencia; espera accesible | Transacciones, lectores, promoción y restauración única comprobados con PostgreSQL; piloto pendiente |
| B1 | Runtime común para mensajería y Web Chat, con aislamiento, idempotencia y outbox | Runtime, persistencia, replay y recibos autenticados Web Chat comprobados con PostgreSQL/Socket.IO; la salida normal de Web Chat pasa por una admisión local durable con procedencia privada y recibo por inbound (`d2ffdf41`); outbox de canales externos, piloto visual y entrega de proveedores pendientes |
| B2 | Prueba/simulación con las mismas reglas de dominio y adaptadores sin efectos externos | Quince writers canónicos de citas, clases, matrícula, taller, catálogo y mascotas; ID de inbound real compartido con runtime/ledger; familias restantes bloqueadas |
| C1 | Slots tipados; preguntas, pausas, correcciones y cambio de intención sin perder misión | Árbitro común, owner por puerto, consentimiento por misión, corrección/reanudación en cuatro idiomas y Flow probados con PostgreSQL; lenguaje arbitrario y piloto pendientes |
| C2 | Ciclos completos de cita, pedido, matrícula/gimnasio; recuperar objetos propios y operaciones inciertas | Pruebas de manejo consolidadas en agenda; registro/corrección de mascotas con recibos atómicos y propietario comprobados con PostgreSQL; históricos y cierres de las demás familias pendientes |
| C3 | Dependencias de herramientas, contrato MCP revisado y selección por tarea completa | Contratos MCP revisados y dependencias base implementados; cobertura completa por tarea pendiente |
| D1 | Recuperación común, procedencia/autoridad/vigencia/scope preservados; precios solo de fuentes válidas | Muestreo de conflictos, citas, revisión humana con CAS y anotaciones RAG implementados; cobertura acotada explícita, sin certificar veracidad global |
| D2 | Indexación por versiones con recuperación; memoria corregible y borrado de derivados | CAS de precisión completa, recuperación, fusión/conflictos de identidad y borrado comprobados con PostgreSQL/pgvector/Prisma reales; calidad semántica y carga pendientes |
| D3 | Diagnósticos de conocimiento correctos y atribución que distingue relevancia de uso real | Atribución observable y diagnóstico técnico implementados y probados con PostgreSQL; no equivalen a veracidad ni entailment |
| E1 | Packs positivos/negativos y verificadores de resultados, incluido taller; cobertura explícita | Verificadores y positivos de citas/clases/matrículas/taller/catálogo/automotriz/mascotas; matriz actual con 32 tareas sin positivo propio y 10 sin verificador; restantes familias pendientes |
| E2 | Revisiones congeladas, canal real y errores contados en regresión/simulación | Contexto del núcleo, FAQs/políticas y temporalidad integrados; réplica RAG administrada, consumo automático, jueces y retención integrados y probados en `5f036d10`; lectores comerciales, reloj y evaluación bajo tráfico/carga pendientes; manifiesto integral conservado |
| E3 | Presupuesto y cola recuperables; candidato/piloto/publicado; promoción, invalidación y rollback | Borrador, candidato y revisión implementados; almacenamiento de publicación/rollback probado sin endpoint; 16 herramientas con autoridad de versión, incluido pago con admisión durable, y entrega aprobada de imágenes/enlaces de Web Chat protegida; la **respuesta normal** de Web Chat y el handoff que ocurre durante su turno tienen admisión y recibo durables (`64dc8d75`, `d2ffdf41`, `02dc2e9e`); outbox durable de salida externa, transporte estricto de WhatsApp, admisión con guardas y recuperación implementados y probados con PostgreSQL (`d5d58216`, `4cf95267`, `c0ed2dc6`, `7ed373be`, `3e7f78db`) **sin productor que los use todavía**; adopción por `sendResponse`, medios/enlaces/Flow como ítems propios, demás canales, publicación HTTP, piloto y promoción/rollback integral pendientes |
| F1 | Misión por plantilla y assessment común con evidencia, dependencias, acciones y pruebas | Assessment común implementado y probado; certificación de tareas reales pendiente |
| F2 | Assist ejecuta acciones tipadas con diff, permisos, versión, idempotencia, auditoría y relectura | Persona, misión, herramientas y horarios de cuenta implementados con revisión, CAS y relectura; validación visual pendiente por fallo de herramienta del navegador |
| F3 | Onboarding y tours por tareas verificadas; continuidad, accesibilidad, móvil e i18n en 4 idiomas | Tours por tareas verificadas implementados; prueba con usuarios nuevos pendiente |
| F4 | Tarjeta/editor/Salud/Assist coherentes; desconocido distinto de ausente; rutas recuperables | Superficies alineadas y errores de carga recuperables; fixtures comprobados por HTTP, revisión visual pendiente |
| G1 | Importar/segmentar chats con linaje, privacidad, deduplicación y separación de fuentes/memoria/estilo | Versión exacta de Inbox, autoridad por intento, CAS de workers, evaluador y retiro de copias tras crash implementados; otras salidas/trazas, despacho diferido, costo/deadline y calibración real pendientes |
| G2 | Curación multidimensional, revisión humana, ejemplos contextualizados y retiro de derivados | Curación y revisión en cuatro idiomas implementadas; revisión humana de muestra pendiente |
| G3 | Dataset reservado sin contaminación; comparar candidato, publicar gradualmente y revertir | Comparación por runtime y evidencia de ledger/objeto/replay en seis familias, incluida ficha/recibo de mascotas; publicación gradual del aprendizaje y retiro implementados; familias restantes y piloto pendientes |
| H1 | Matriz capacidad/tarea/datos/comando/verificador/canal por perfil; sin certificación heredada | Matriz declarativa por 76 perfiles / 268 tareas; 146 transaccionales; evidencia ejecutada y certificación por perfil pendientes |
| H2 | Fallos reales a regresiones; métricas con denominadores por misión, idioma, canal y dificultad | Regresiones revisadas desde QA/ledger, linaje y retiro de derivados, identidad de misión y denominadores implementados; resultado operativo desconocido conserva ese estado, sin tasa inventada |
| H3 | Pilotos completos con tenant/backend/proveedores, usuarios nuevos y comparación de desempeño | Pruebas de PostgreSQL aislado ejecutadas; proveedores, usuarios nuevos y benchmark sin ejecutar |

## Verificación y commits

Cada entrada registrará archivos, comportamiento comprobado, comandos ejecutados y límites. Un test unitario no certifica por sí solo una vertical ni un proveedor real. La finalización del programa exige revisar cada requisito de esta tabla contra evidencia vigente.

### Primera tanda de integridad

- `ae84eec2`: conserva auditoría, reproducciones y alcance completo de aceptación.
- `13dfedd6`: contratos de borrador, campos de procedimientos y reporte de conocimiento tipados.
- `9ad42aee`: consentimiento de mensaje completo; referencias canónicas firmadas con la propuesta; rechazo de cambios de términos y de reutilizar el mismo mensaje para autorizar; slots tipados y preguntas/pausa/cancelación; fechas civiles independientes del TZ del proceso. 11 suites focalizadas / 194 pruebas locales pasan.
- `fc60a721`: herramienta de citas usa comando canónico; anticipo/retención/referencia; settlement con pago persistido, capacidad y outbox bajo transacción; recuperación de pago tardío; aislamiento de notificaciones/calendario de evaluación. Matrícula y gimnasio usan transacciones, espera accesible y lector de reservas propias. 13 suites / 101 casos locales; bootstrap NestJS pasa.
- `5f7ceed2`: Web Chat ejecuta `generateResponse`, igual que mensajería; preserva contacto/canal/inbound, cuota, lock y respuesta reutilizable; transporte persiste antes de emitir. Borrador consulta lectores auditados y bloquea motores/escrituras/handoff/media externos. Los precios se validan exclusivamente con conocimiento recuperado y campos monetarios del dominio, excluyendo historial del cliente/modelo. Pruebas focalizadas de integración, precios, widget y selección de herramientas pasan; TypeScript API y dashboard verificados durante la tanda.

### Runtime, conocimiento, evaluación y aprendizaje

- `ca76f15f`: indexación por versiones con compare-and-swap; filtros de agente/audiencia/jurisdicción; memoria con procedencia y corrección atómica; borrado y tombstones; diagnóstico de huecos de conocimiento en cuatro idiomas. 5 suites / 35 pruebas focalizadas pasan.
- `a1f0c1cb`: contratos MCP revisados ligados a definición, endpoint y credencial; alcance de contacto/tenant ligado por el servidor; confirmación e identidad por efecto; timeout de escritura como resultado incierto sin repetición automática. Las herramientas leídas no justifican afirmar una escritura. No hay certificación de un proveedor MCP externo.
- `a9a32c65`: snapshot inmutable de configuración; ejecución por canal; errores en el denominador; checkpoints, recuperación de trabajos y presupuesto durable por tenant. 8 suites / 61 pruebas pasan. El presupuesto usa unidades conservadoras de llamadas de modelo; no representa un importe en dólares.
- `75fe01c3`: tareas de inicio verificadas contra backend; tours interactivos que preparan pantalla/formulario y vuelven a comprobar el resultado; revisión MCP visible. Backend focalizado: 2 suites / 65 pruebas; dashboard focalizado: 4 suites / 52 pruebas. Una ejecución previa del dashboard completo pasó 33 suites / 325 pruebas.
- `afe7c009`: estado de procedimiento persistido antes de actuar, PostgreSQL como autoridad y Redis como caché; pausa/retomar; misión de reserva recuperable; botones ligados a la propuesta y sus términos vigentes. No confundir duración de misión con vigencia de consentimiento.
- `353dd0f2`: resolución de capacidades de MCP, vertical, país, plan y pagos respeta contexto de prueba sin reparar/escribir caches, credenciales ni tablas. 3 suites / 11 pruebas específicas pasan; batería combinada de continuidad/consentimiento/pagos/MCP: 12 suites / 148 pruebas pasan.
- `9fafa46a`: contratos compartidos de misión, assessment, cambios revisables y trazas por turno.
- `876723d7`: Agent Test deja de tener un pipeline propio y utiliza el runtime operativo con adaptadores de sesión, estado, transporte, herramientas y trazas. Aprendizaje con importación, privacidad, separación por cliente, deduplicación semántica, revisión, snapshots, comparación A/B por runtime real, publicación gradual y retiro. El borrado bloquea claims/finalizers concurrentes e invalida snapshots y outbox identificables. Runtime: 15 suites / 104 pruebas; aprendizaje/privacidad: 9 suites / 106 pruebas pasan. API TypeScript y bootstrap de AppModule pasan.
- `d07d158e`: pantalla para importar desde bandeja o texto, revisar calidad y evidencia, seleccionar ejemplos, comparar, publicar gradualmente y retirar versiones. Traducciones es/en/pt/fr; el original cargado permanece solo en la visita del navegador. El conjunto reservado no se expone en la API de revisión. 2 suites / 19 pruebas de parser, aprobación y render en cuatro idiomas pasan; dashboard TypeScript pasa.
- `2b686499`: verificador de efectos que falla ante SQL inválido, conteo ausente o assertions desconocidas; lector de órdenes de taller sin habilitar su writer; matriz declarativa de todos los perfiles y generador CSV/Markdown. Los escenarios llamados happy_path que sólo comprueban ausencia de escrituras no cuentan como éxito operacional.
- `751d23e7`: assessment común en inicio, editor, salud y Assist; misión por plantilla; comandos tipados de persona, herramientas y horarios, con diff exacto, versión, permisos, idempotencia y auditoría. Los editores clásicos también usan CAS. Horarios afectan a toda la cuenta y actualizan las versiones de todos los agentes. API: 23 suites / 308 pruebas; dashboard: 37 suites / 358 pruebas; TypeScript de ambas aplicaciones pasa.
- `fc6b42c7`: atribución sobre la respuesta generada con IDs de recuperación/lote/versión y señales de cita o coincidencia literal. Se excluyen tasas históricas que confundían relevancia con uso. Estado técnico y razones de revisión reemplazan puntajes inventados de calidad documental. 6 suites / 88 pruebas y 4 pruebas de PostgreSQL pasan, incluidas agregación por búsqueda y borrado concurrente. No acredita entrega ni exactitud factual.
- `232a807b`: borrador registra propuestas en el ledger, solicita consentimiento al cliente cuando corresponde y exige después aprobación humana. Congela agente/versión/argumentos y mensaje origen, revalida expiración y versión antes de adquirir la ejecución; el turno de borrador nunca ejecuta el writer. Preparación de pagos usa importe/referencia canónicos. 9 suites / 109 pruebas pasan; el recorrido completo también se verificó con PostgreSQL en la siguiente tanda.
- `63b3c024`: namespaces temporales vacíos con secuencias/FK independientes, leases de propiedad y limpieza restringida; siete writers reales de citas/clases/matrículas usan comandos y guardas canónicos, sin efectos externos. Fixtures con fecha y horario derivados del snapshot; casos completos y recuperación en cuatro idiomas. Corrige horas inexistentes/ambiguas, concurrencia de reprogramación, y errores de casts SQL encontrados por PostgreSQL. Batch de integración: 13 suites / 129 pruebas; tanda temporal y escenarios: 19 suites / 194 pruebas; dentro de la evidencia hay 22 pruebas reales de PostgreSQL (no sumar conteos solapados). Shared build y API TypeScript pasan. Evidencia detallada en `docs/audits/2026-09-07/canonical-eval-isolation.md`.
- `a57b59a3`: revisión de acciones en Inbox, también en móvil, con argumentos, permisos, expiración y estados separados de aprobación/ejecución/negocio. POST incierto se verifica mediante GET; no se infiere éxito por finalizar el workflow. 26 pruebas y dashboard TypeScript pasan. La entrega al proveedor se implementa como un estado adicional.
- `f479ac12`: hasta cuatro procedimientos pausados además del actual; selección explícita por nombre, slots separados, definición/versiones revalidadas y retorno pausado al concluir otra tarea. Rechazo, pregunta, cancelación, expiración y opt-out no ejecutan tareas suspendidas. 5 suites / 71 pruebas pasan. Todavía no unifica todos los motores de misión.
- `3da6f28b`: diferencia entrega aceptada por proveedor, resultado del negocio y workflow completado; fallos al cargar aprendizaje muestran estado desconocido y reintento. Corrige recomendación de crear agente cuando ya existe. 3 suites / 51 pruebas y dashboard TypeScript pasan. Fixture local y reproducción documentados en `docs/agent-platform-visual-review-2026-09-07.md`; el navegador no pudo inicializarse y no se declara revisión visual ejecutada.
- `6f8dce70`: efectos aprobados persistidos con ledger/outbox, validación de identidad y canal, fence de privacidad y recuperación sin repetir el comando. Un envío iniciado pero incierto requiere conciliación; aceptación del proveedor no significa entrega/lectura. 16 suites / 117 pruebas, incluidas 12 de PostgreSQL, y bootstrap pasan. Evidencia en `docs/audits/2026-09-07/approved-tool-delivery.md`. Transporte asíncrono de Web Chat continúa en ejecución.
- `609448e2`: el comando de citas compara los términos aceptados bajo lock del servicio y conserva precio, moneda, duración, lugar y anticipo en la reserva; el cobro y lectores usan esos términos. Una edición concurrente espera al commit y una propuesta anterior exige revisar el cambio. 15 suites / 176 pruebas focalizadas y tanda de 13 pruebas PostgreSQL pasan (conteos solapados); API TypeScript pasó durante la tanda. Evidencia en `docs/audits/2026-09-07/appointment-accepted-terms.md`. No reconstruye condiciones históricas ausentes ni certifica términos de otras familias.
- `1ea2bf75`: Agent Test, Eval, Simulation y aprendizaje comparten manifiesto integral de dependencias, con firmas bajo MVCC, inputs privados congelados y guardas de lectura. Los cambios de contenido, embeddings, tablas nuevas, DDL, routing y escenarios invalidan evidencia anterior. Corrige bootstrap de snapshots con versión de caché nueva. 17 suites / 174 pruebas pasan; tras la corrección de caché, 2 suites / 19 pruebas pasan. API TypeScript verificado en integración. Estrategia `guarded_live_dependencies`, con reloj/pesos externos sin congelar y costo de firmas todavía sin benchmark. Matriz regenerada: 76 perfiles, 254 tareas, 139 transaccionales, 52 sin positivo propio y 20 sin verificador; cero perfiles certificados.
- `020e6437`: conflictos de conocimiento con citas exactas, cuatro tipos de fuente, fuente/versiones/scope ligados al caso y decisiones humanas con actor/motivo/CAS; cambios de fuente invalidan preferencias. RAG recibe anotaciones acotadas; errores de muestreo o del juez se muestran como desconocidos. 9 suites / 91 pruebas API (6 PostgreSQL), 2 suites / 58 pruebas dashboard y TypeScript de ambas aplicaciones pasan. Evidencia en `docs/knowledge-conflict-review-2026-09-07.md`. La muestra de pares no prueba exhaustividad, ni permite reemplazar precios o políticas canónicos con prosa.

- `c5d0719a`: Web Chat persiste respuestas del agente, humanas y aprobadas antes de emitir; autentica sesión, origen y propietario en lectura/escritura/replay/recibo. El recibo del navegador cambia pendiente a entregado, sin afirmar lectura humana. Borrado retira tokens y pre-chat. 28 suites / 198 pruebas pasan; ajuste final de lease: 2 suites PostgreSQL / 28 pruebas pasan, con Socket.IO real y loader en VM. No hay prueba visual. Evidencia en `docs/audits/2026-09-07/web-chat-durable-delivery.md`; la entrega a proveedores externos sigue sin piloto.
- `d2ca53b9`: QA conserva revisión, mensajes, hash, rúbrica y cobertura; descarta resultados tras edición/borrado y deduplica workers. Separa opinión conversacional de resultado operativo desconocido; excluye verificaciones antiguas sin evidencia del cálculo gestionado. 7 suites API / 104 pruebas durante la tanda; después se agregaron dos regresiones de bootstrap con Prisma real y la suite PostgreSQL completa pasó 14 pruebas. Dashboard global: 42 suites / 434 pruebas pasan al integrar QA y Sampling; API TypeScript pasa. Evidencia en `docs/audits/2026-09-07/production-quality-evidence.md`.
- `28ba3f44`: Watchtower captura una muestra diaria y su denominador en PostgreSQL, incluye solicitudes sin respuesta y traspasos, persiste reintentos y publica trabajos con IDs estables. Cobertura ausente es desconocida; aceptación de la cola no significa evaluación terminada. 2 suites API / 13 pruebas pasan, incluidas 10 PostgreSQL y una prueba de ACK perdido después de completar un worker BullMQ/Valkey real. 6 pruebas UI y TypeScript verificados; cuatro idiomas. Evidencia en `docs/audits/2026-09-07/quality-sampling.md`.
- `7ab2f133`: CAS de indexación conserva microsegundos; extracción revalida identidad y memoria vigente; fusión mueve hechos y metadata en transacción, preserva conflictos y exige aclaración posterior para resolverlos. Borrado alcanza los hechos legados y derivados. 9 suites / 71 pruebas pasan, incluidas 19 con PostgreSQL/pgvector y serialización Prisma reales; API TypeScript pasa. Evidencia en `docs/knowledge-memory-publication-review-2026-09-07.md`.
- `403ce610`: elimina una llamada residual del gate que insertaba el contacto de fixture en el esquema operativo antes de abrir el namespace aislado. Dos regresiones de orquestación comprueban escrituras de fixtures exclusivamente en el namespace, tanto en éxito como en fallo del proveedor; 4 suites / 33 pruebas pasan. No borra contactos históricos ni presupone que un identificador reservado autoriza su eliminación.
- `edb4383e`: mide `capture` y `assertCurrent` reales sobre PostgreSQL/pgvector/Prisma aislados. Firmas completas con 44 tablas y conjuntos sintéticos de 100, 1.000 y 5.000 mensajes: capturas calientes de 45–123 ms y verificaciones de 42–121 ms en este host. No es un SLO ni una medición de conversación completa; datos compresibles y sin proveedor LLM. Evidencia y script en `docs/audits/2026-09-07/evaluation-revision-benchmark.md`.
- `17303202`: taller usa términos canónicos y versión para aprobar/rechazar presupuesto o cancelar; evita duplicar vehículo/orden/eventos en reintentos, conserva privacidad y valida propiedad. Los tres writers usan namespace canónico y escenarios propios en cuatro idiomas. 12 suites / 169 pruebas, incluidas 20 con PostgreSQL/Prisma reales; API/dashboard TypeScript pasan. El diagnóstico técnico y la entrega final siguen siendo trabajo humano. Evidencia en `docs/repair-orders-runtime-review-2026-09-07.md`.
- `a77a6be8`: confirmación de cita pagada y promociones de gimnasio/educación conservan avisos en outbox transaccional; el transporte distingue persistencia, aceptación e incertidumbre. Lista de espera educativa con términos aceptados, FIFO, capacidad y consentimiento nuevo tras perder el cupo. 16 suites / 112 pruebas durante la tanda; integración final de SMTP/core/cola: 5 suites / 16 pruebas pasan. Borrado integrado en Compliance. Evidencia en `docs/audits/2026-09-07/operational-notices-and-waitlists.md`. Falta interfaz y resolución operativa de avisos inciertos.

- `0f87453e`: almacén de revisiones editables separado del agente operativo, con base, hash, CAS e idempotencia; preparación de la frontera de publicación sin alterar routing ni aprobaciones vigentes.
- `c1fef761`: fallos observados se convierten en regresiones sólo tras revisión explícita de fuente, reproducción y privacidad. La evidencia queda ligada a versiones y se invalida tras cambio/borrado. Identidades y denominadores de misión distinguen fin del workflow de resultado del negocio; política de release exige casos canónicos por misión, idioma y canal. 8 suites / 99 pruebas, incluidas 25 PostgreSQL; dashboard global 43 suites / 443 pruebas y TypeScript del índice de API/dashboard pasan.
- `c3f4a061`: el gate lee el inventario efectivo después de capturar la revisión y vuelve a comprobar dependencias; un escenario incorporado durante la captura no se omite. 11 pruebas de infraestructura pasan.
- `3b3927f3`: arbitraje común entre motores, consentimiento ligado a misión y puerto, correcciones/reanudación, Flow y checkpoints protegidos por borrado. Pedidos transaccionales con cotización canónica, idempotencia, stock por línea, consulta propia, cancelación y revisión histórica; API, web y móvil usan el contrato. Eval/Simulation/Learning conservan el ID de inbound persistido; aprendizaje mide en su namespace. Integración focal: 7 suites / 113 pruebas, incluidas 55 PostgreSQL; móvil 6 suites / 88 pruebas; TypeScript del índice API/dashboard/móvil pasa. La matriz pasa a 76 perfiles, 268 tareas, 146 transaccionales, 42 sin positivo propio, 20 sin verificador y cero certificados.

- `d50d1670`: editor, wizard y Assist guardan revisiones editables separadas; candidato congelado común para todos sus canales, lease/checkpoints/presupuesto y recuperación de cola, revisión humana con CAS/evidencia y retiro por privacidad. La activación sigue separada. Draft: 8 suites / 132 pruebas (33 PostgreSQL); integración release: 8 suites / 94 pruebas (35 PostgreSQL); dashboard global 45 suites / 460 pruebas y TypeScript del índice API/dashboard pasan.
- `6f5af2e8`: aprendizaje verifica ledger y objeto exacto del contacto/conversación; un replay válido no necesita otra mutación y cambios ajenos no prueban éxito. 22 pruebas en cuatro suites, incluidas cinco PostgreSQL/Prisma; tres ciclos adicionales del executor con PostgreSQL. TypeScript del índice API pasa. Evidencia en `docs/learning-operation-evidence-review-2026-09-07.md`.
- `8a61c7aa`: selección operativa lee configuración y versión en una misma consulta, sin caché que resucite agentes desactivados ni agente fabricado por defecto. Mensajes sin persona activa se conservan en Inbox; propuestas de borrador no asocian instrucciones viejas con versión nueva. 38 pruebas en seis suites, incluidas siete PostgreSQL; una regresión adicional sobre cambio durante el turno pasó después, con su suite de siete casos. TypeScript del índice API pasa.
- `695f8d0d`: revisión del candidato muestra proyección de configuración capturada, diferencias, muestras y verificaciones de herramientas/objetos selladas en evidencia. Cambios de versión o fuente retiran la evidencia anterior. API 43 pruebas, incluidas 21 PostgreSQL; dashboard global 46 suites / 474 pruebas; TypeScript del índice API/dashboard pasa. No habilita publicación ni supone revisión visual de navegador.
- `61ad6e59`: readiness y alojamiento omiten cachés productivas en evaluación y usan el namespace elegido; lectura de ownership de ChannelManager no descifra secretos. Inventario de lectores/puertos y capturador piloto de servicios con MVCC, integridad y caducidad. 10 suites / 121 pruebas, incluidas 11 PostgreSQL; TypeScript del índice API pasa. El capturador todavía no reemplaza ningún lector del runtime ni el manifiesto global.

- `fbede27b`: primitiva interna de publicación/rollback con receipts, CAS, locks, routing sin reasignación implícita y prerrequisitos frescos de tenant, plan, capacidad y agenda. 28 pruebas PostgreSQL de release/publicación, 12 unitarias y cuatro de prerrequisitos con base aislada pasan; seis pruebas adicionales del probe de concurrencia documentan por qué un lock externo no protege efectos que usan otra conexión. TypeScript del índice API pasa. Sin endpoint ni activación de tenant; evidencia en `docs/audits/2026-09-07/agent-publication-storage.md`.
- `b6db0507`: revisión de avisos inciertos en API e interfaz, roles revalidados, CAS, idempotencia, evidencia separada de referencias humanas, supresión sin reenvío y borrado de notas derivadas. 40 pruebas API en tres suites (28 PostgreSQL/Prisma) y 89 de UI/navegación en tres suites pasan; TypeScript del índice API/dashboard pasa. No consulta por sí sola un proveedor externo ni afirma recepción a partir de una nota humana.

### Reconciliación vigente de inventario, RAG administrado y pagos

- `e398be7b`: reconcilia el inventario de lectores y capturas con los bloques registrados y sus pendientes de integración. El inventario describe cobertura; no certifica perfiles ni retira las dependencias conservadoras de E2.
- `aed7f994`: gestor de ciclo de vida de réplicas RAG, espacios reutilizables y referencias de uso con propiedad y vencimiento. **55 pruebas pasan**. Este bloque ya está registrado; su integración automática con snapshots, consumidores, jueces y limpieza de sus recorridos no se deduce de esa validación.
- `ea868d15`: `create_payment_link` transmite autoridad privada hasta el intent, procesamiento, reserva y admisión transaccional; **18 suites / 183 pruebas pasan, incluidas 20 PostgreSQL/Prisma**. El COMMIT de admisión autoriza una tentativa, sin locks durante el POST. Una versión retirada antes de admitir produce cero llamadas; después se conservan recibos y settlement. Un ACK incierto exige conciliación sin recrear el enlace. Son **16 herramientas con autoridad operativa**, mientras el sandbox mantiene **15 writers auditados**: pagos no se habilita en evaluación.

- `5f036d10`: integra la referencia RAG administrada en Agent Test, Eval, Simulation, Learning, candidatas, jueces y retención de esos recorridos. **Validación del índice exacto: TypeScript API pasa; 42 suites / 610 casos pasan, cero omitidos, incluidas 17 suites PostgreSQL / 273 casos.** Estas cifras incluyen las suites sensibles de los bloques anteriores y no se suman a ellas. Autorun invalida fuentes vencidas y conserva el resultado terminal ante ACK incierto; libera sólo su referencia exacta tras confirmar el estado y fuera del fence de fuente. La limpieza diferida o fallida, las solicitudes sustituidas y las persistencias inciertas pueden conservar referencias hasta su TTL fijo.

- `e8cea802`: entrega aprobada de imágenes/enlaces de Web Chat ligada al origen privado de la propuesta, con mensaje y recibo atómicos. Conserva recibos aceptados después de publicar otra versión, pagos canónicos y respuestas humanas; rechaza vínculos cambiados y limita fallos de persistencia/disponibilidad sin degradar un COMMIT confirmado. **TypeScript API y 10 suites / 90 casos pasan sobre el índice exacto**, incluidos 65 PostgreSQL en tres suites, sin omitidos. Alcance en [Autoridad de entregas aprobadas](audits/2026-09-07/approved-webchat-agent-authority.md).
- `92f9d2fa`: la misma entrega comprueba el agente seleccionado por conexión, incluso si otro agente toma prioridad sin cambiar la versión anterior. El scope privado permite una propuesta legítima del segundo agente sin cambiar la atribución histórica de la conversación. **TypeScript API y 10 suites / 94 casos pasan sobre el índice exacto**, incluidos 69 PostgreSQL, sin omitidos. La reanudación de la propuesta se corrige en un bloque independiente. El [plan de salida normal](audits/2026-09-07/normal-agent-dispatch-authority-plan.md) detalla los cambios de recuperación, admisión y recibos aún pendientes.
- `9e5016f8`: reanudación con selector canónico y scope original; verifica tenant/esquema en el contexto y en la transacción final. **TypeScript API y 16 suites / 195 casos pasan sobre el índice exacto**, incluidos 91 PostgreSQL; los siete casos inicialmente omitidos se ejecutaron después sobre la misma copia. [Evidencia y límites](audits/2026-09-07/approval-resume-routing.md).
- `3d9c04ea`: corrige una expectativa antigua del test del adaptador: una autoridad pedida por el turno debe conservar también el control obligatorio de fuentes de la sesión. **TypeScript API y siete pruebas pasan sobre el índice exacto**. Prueba composición, rechazo de cualquiera de las fuentes y deduplicación del mismo callback; no cambia código de producción.
- `54379f90`: procedencia privada serializable del aprendizaje y comprobación sobre la conexión suministrada; lectura reutilizada en callbacks actuales, admisión con bloqueos hasta COMMIT preparada para futuros consumidores. Conserva todas las fuentes de entrenamiento/holdout, Inbox, hashes y versiones. **TypeScript API y 11 suites / 200 casos pasan sobre el índice exacto**, incluidos 86 PostgreSQL, sin omitidos. Corrige los datos incompletos del fixture Replay sin cambiar su control productivo. [Contrato, evidencia y límites](audits/2026-09-07/runtime-learning-footprint.md).
- `1f704ddc`: tablas de procedencia de respuestas normales Widget y borrado de derivados por destinatario, fuente o versión; Compliance y retiro de aprendizaje conservan una constancia mínima del inbound para evitar regeneración tras borrar. **TypeScript API y 14 suites / 220 casos pasan sobre el índice exacto**, incluidos 97 PostgreSQL/Prisma en cuatro suites, sin omitidos. El productor normal de recibos no está integrado y no se migraron tenants existentes.
- `735a0511`: colector privado de proyecciones acumuladas durante la respuesta, incluidas fuentes históricas de otros agentes del mismo tenant; validación en el fence compartido y lector privado de historial que distingue ausencia de seguimiento de una respuesta borrada. **TypeScript API y 13 suites / 228 casos pasan sobre el índice exacto**, incluidos 86 PostgreSQL/Prisma en tres suites, sin omitidos. No activa la integración de core/Gateway. [Evidencia de ambos commits y borrador preservado](handoffs/2026-09-08/claude-execution-handoff.md).

El alcance y los límites se documentan en [Conocimiento administrado en evaluaciones](audits/2026-09-07/evaluation-managed-knowledge-runtime.md); no se declara completado E2 ni E3. La matriz mantiene **cero perfiles certificados**.

### Evolución posterior a `b6db0507` — registro histórico de tandas

Las entradas siguientes conservan la evidencia y los límites de cada etapa, incluidas sus baterías solapadas. Los estados del índice y el bloqueo de commits que existieron durante esas tandas son históricos. El estado vigente está en la tabla de requisitos y en los frentes abiertos; la secuencia por hash se mantiene en la bitácora de commits incrementales enlazada arriba.

Al reconciliar sobre `51886bb8`, contexto/FAQs/políticas, temporalidad, fuentes de aprendizaje, capturador RAG, vehículos, mascotas y Replay ya estaban registrados. En ese corte, la validación integrada de RAG fue **10 suites / 103 casos API**; vehículos, **19 suites / 274 API y 2 / 29 dashboard**; mascotas, **26 suites / 389 API en una ejecución completa**; Replay, **14 suites / 197 API y 2 / 27 dashboard**. TypeScript de las aplicaciones correspondientes pasó sobre las copias exactas del índice. Estos resultados no se suman a las baterías históricas ni certifican un perfil, una entrega externa o un despliegue. El gestor del ciclo de vida RAG se registró después en `aed7f994` y su integración con consumidores en `5f036d10`, con la evidencia separada indicada en la reconciliación vigente.

- E2 / réplica RAG: copia relacional en PostgreSQL de siete proyecciones, sin transportar corpus/vectores en el snapshot; referencias selladas, lectura readonly con hash/lease/vínculo de tenant, compatibilidad vector-only y rechazo sin truncamiento de datos demasiado grandes. `KnowledgeService` admite la referencia privada y conserva búsqueda/filtros/conflictos canónicos; se corrigen tipos de resultados de consultas preparadas y desempates. **10 suites / 99 casos pasan**; tras preservar los tipos temporales del origen, la suite afectada pasa sus **21 casos PostgreSQL/pgvector**. TypeScript API y tres pruebas de inventario pasan. En esa etapa Agent Test/Eval/Simulation/Learning no creaban ni seleccionaban automáticamente la réplica. El gestor de ciclo de vida y la integración con consumidores se registraron después en `aed7f994` y `5f036d10`; el presupuesto agregado bajo carga y los demás límites siguen pendientes. Evidencia y secuencia de integración en `docs/audits/2026-09-07/evaluation-knowledge-replica.md`. La tanda se registró después; no hubo despliegue.
- Catálogo: `get_product` distingue producto ausente de consulta fallida y declara estado/frescura; `send_product_image` conserva el fallo del lector sin generar media ni confundirlo con ausencia de fotos. **6 suites / 56 casos pasan**, con seis casos nuevos; TypeScript API pasa. Pruebas con fallos de base simulados, sin certificar PostgreSQL ni entrega externa. La réplica comercial sigue pendiente; el capturador y el gestor RAG tienen sus propias tandas, y su integración automática se registró después en `5f036d10`. Evidencia en `docs/audits/2026-09-07/catalog-read-failure.md`; la tanda se registró después, sin despliegue.
- E2 / contexto temporal: escenarios, slots y configuración sintética usan la precedencia de horarios y zona capturados del núcleo. Creación de citas, disponibilidad y reprogramación en namespace exigen configuración temporal única y lease vigente; no aceptan metadata ni región pública como fallback. **17 suites / 206 casos pasan**, incluidos siete PostgreSQL nuevos y un recorrido de fixtures/creación con Prisma real; TypeScript API pasa. Continúan reloj global, réplica comercial y armonización temporal de todas las superficies operativas. La integración RAG en consumidores se registró después en `5f036d10`. Evidencia en `docs/audits/2026-09-07/evaluation-temporal-context.md`. La tanda se registró después; no hubo despliegue.
- E2 / conocimiento estructurado: FAQs publicadas y políticas activas capturadas en una transacción MVCC readonly, selladas en el snapshot y consultadas por los lectores canónicos mediante recordsets PostgreSQL parametrizados. Ausencia, vacío y fallos se distinguen; ningún argumento del modelo sustituye la captura. Se corrige además la prioridad de vectores nulos y el desempate de FAQs en el lector vivo. **18 suites / 214 casos pasan**, incluidos 14 PostgreSQL/Prisma nuevos; TypeScript API pasa. La integración RAG en consumidores se registró después en `5f036d10`; la réplica comercial, la retención de las restantes salidas y la carga continúan pendientes. La diferencia de horarios de fixtures detectada en esta tanda se corrige en `evaluation-temporal-context.md`. Evidencia y límites en `docs/audits/2026-09-07/evaluation-structured-knowledge-capture.md`. La tanda se registró después; no hubo despliegue.
- Mascotas: comando común de registro/corrección/desactivación con especie explícita, validación de campos, propietario y versión bajo transacción, recibos atómicos, recuperación sin duplicados y retiro de recibos derivados. Sandbox y aprendizaje verifican la ficha contra el comando; confirmaciones de corrección en cuatro idiomas. La prueba Prisma descubrió y corrigió el cast de fecha. **26 suites / 373 casos comprobados por bloques**; última batería afectada: tres suites PostgreSQL / 60 casos. TypeScript API/shared pasa. Matriz regenerada: **76 perfiles / 268 tareas / 146 transaccionales / 32 sin positivo / 10 sin verificador / cero certificados**. Los 192 escenarios de mascotas están declarados, aún sin ejecución LLM por revisión/canal/idioma. Límites de consentimiento declarativo, originales clínicos y pilotos en `docs/audits/2026-09-07/pet-command-lifecycle.md`. Esa evidencia histórica fue ampliada por la validación integrada indicada arriba; no hubo despliegue.
- Fuentes en salidas externas: embeddings de análisis/holdout, RAG automático, herramienta de conocimiento y memoria conservan la autoridad de fuente y worker; el router saca los reintentos de los SDK para comprobar cada envío. Una fuente inválida no se convierte en fallback válido de evaluación. Verificación por bloques: 19 suites / 254 casos, incluidos 51 PostgreSQL de fuentes/worker, 19 de publicación/memoria, dos de namespace y 28 de transporte con SDK reales e HTTP simulado. Se actualizó un fixture SQL antiguo; TypeScript API pasa. Evidencia, comandos y límites en `docs/audits/2026-09-07/learning-external-source-authority.md`. Continúan costo integral, deadline/reintentos bajo carga, otras salidas externas y calidad semántica; la tanda se registró después, sin despliegue.
- Workers de aprendizaje: identidad nueva por invocación, reclamación con CAS después del lease del sandbox, lectura transaccional del último checkpoint y token obligatorio en copias, llamadas de generación y escrituras posteriores. Reclamaciones/fallos tardíos y recuperación de cola no afectan a una generación nueva. Cinco suites / 83 casos pasan, incluidos 44 PostgreSQL/Prisma y bootstrap; tras reforzar namespaces y CAS, los bloques afectados pasan 82 y 59 casos respectivamente, con solapamiento. TypeScript API pasa. Detalle y límites en `docs/audits/2026-09-07/learning-worker-ownership.md`; la tanda se registró después, sin despliegue.
- Evaluación de aprendizaje: índice durable antes de copiar chats, autoridad por intento para replay/juez e interpretación/búsquedas auxiliares, fuentes de candidata y baseline verificadas al guardar, retiro de revisiones descendientes y limpieza por ownership exacto. La recuperación usa el marcador real de vencimiento e incluye tenants inactivos sin reactivar sus trabajos; `to_regclass::text` se probó mediante el recorrido de Prisma. Cobertura ejecutada: 11 suites / 131 casos, incluidos 40 PostgreSQL de fuentes, dos de namespace y cinco de manifiesto. Últimos bloques: tres suites / 74 casos y cinco / 56, con solapamiento; TypeScript API pasa. Evidencia y límites en `docs/audits/2026-09-07/learning-evaluation-retention.md`. La tanda quedó registrada posteriormente; la validación exacta de su bloque se conserva en la bitácora incremental.
- Revisión de candidato: snapshot completo de canales, conexiones exactas, horario y activación, íntegramente ligado al hash de la revisión; comparativo visible en cuatro idiomas. Cuatro suites API / 58 casos y 14 casos de interfaz pasan; TypeScript del índice API/dashboard pasa. Al preparar esta tanda, la revisión automática rechazó su commit por límite de uso; posteriormente se registró el bloque y se verificó su contenido exacto.
- Replays de Simulation: captura exacta de mensajes fuente, autoridad evaluativa, retiro de baselines y copias temporales tras crash, guards de modelos/herramientas/judge, endpoint y pantalla de retiro. Se corrigió además un posible ciclo de locks al registrar el namespace antes de abrir el fence de uso externo. Una regresión posterior reprodujo Replay → Learning detrás de un borrado pendiente; el helper privado de fuente comparte la misma transacción solo para ese Prisma/schema, mantiene las guardas y rechaza scopes cerrados o ajenos. No vuelve reentrantes los comandos de negocio. Validación final: cuatro suites API / 47 casos, incluidos 16 PostgreSQL/Prisma; dos suites de interfaz / 27 casos (14 solapan con candidato). TypeScript dashboard pasa. Evidencia en `docs/audits/2026-09-07/simulation-replay-source-authority.md`.
- Autoridad operativa de doce comandos: mismo tenant/agente/versión/hash dentro de la transacción del efecto en cinco familias; metadata privada en runtime y aprobaciones. Se conservan recibos anteriores a publicación sin reejecutar ni renovar propuestas obsoletas. 17 casos PostgreSQL propios pasan; integración inicial de 150 casos, batería de procedencia de 42 y batería de recibos de 54 pasan en llamadas que se solapan. Detalle y límites en `docs/audits/2026-09-07/served-agent-effect-authority.md`. No habilita todavía publicación HTTP.
- Automotriz: alias del agente y API de inventario consolidados en `appointments`; identidad durable, términos, capacidad de vehículo/asesor, listado, reprogramación, cancelación y settlement con estados fieles. Módulo compartido de comandos, interfaz e i18n incluidos. Batería de 11 suites / 197 casos pasa, incluidos 15 PostgreSQL/Prisma y ocho de admisión del alias. Después se conectaron inventario, permisos y requisitos de agenda con Salud/assessment/onboarding: cuatro suites API / 82 casos pasan, incluidos 16 PostgreSQL; trece suites dashboard / 127 casos pasan. Las cifras se solapan. API/dashboard pasan TypeScript y shared compila. Los escenarios, el verificador y el directorio aislado se completaron en la tanda automotriz posterior. La recuperación histórica, el tour específico de permisos y el piloto siguen pendientes. Evidencia y límites en `docs/audits/2026-09-07/vehicle-appointment-consolidation.md`. Esa batería inicial no habilitaba por sí sola el sandbox; la admisión posterior del alias tiene su propia evidencia. No hay certificación por perfil.
- E2: identidad, horarios, región, plantilla en cuatro idiomas y fallback de exposición de objetos integrados como entradas privadas selladas del núcleo común. Fallos de origen o capturas incompletas detienen la evaluación; los datos ausentes se registran explícitamente. Se conserva la política de dominios desconocidos y no se releen esos datos por turno. **20 suites / 314 casos pasan**, incluidos siete PostgreSQL/Prisma nuevos; TypeScript API pasa. El capturador piloto de servicios sigue sin integrar; el manifiesto global conserva todas sus dependencias. Evidencia y límites en `docs/audits/2026-09-07/evaluation-core-context-capture.md`. La tanda se registró después; no hubo despliegue.
- Validación de la tanda Inbox: cinco suites API / 51 casos (18 PostgreSQL de procedencia y dos de namespace), dos suites de interfaz / 27 casos y TypeScript API/dashboard pasan. Esta fue la validación inicial de Inbox; su integración posterior con runtime, workers y retención quedó registrada y mantiene sus propias cifras.
- Aprendizaje durante el runtime: autoridad por intento de proveedor, proyección exacta de ejemplos, validación de todas las fuentes, footprint acumulado y guard común para iteraciones, cierre forzado y reescrituras. Una revocación detiene el fallback con el prompt obsoleto; conserva costo conocido, limpia contexto generado y recupera la respuesta sin repetir herramientas. Las evaluaciones conservan el fallo de la candidata. También se impiden eventos operativos del guard de precios en Agent Test. La tanda posterior de retención cerró el juez/evaluador y las copias temporales descritas en su auditoría. Continúan el despacho diferido y la revisión de las demás salidas/trazas. Detalle en `docs/audits/2026-09-07/learning-runtime-source-authority.md`.
- Aprendizaje desde Inbox: captura coherente del chat e identidad, hashes y mensajes exactos, revisión obligatoria tras cambios/reimportación y retiro de derivados de train/holdout. Los lectores y publicación rechazan fuentes modificadas o legadas sin prueba; el análisis externo mantiene el fence de privacidad y descarta resultados tardíos. Interfaz en cuatro idiomas con reimportación y estados fieles. Se corrigieron lecturas Prisma de vector/regclass. La integración posterior alcanzó el LLM operativo y la retención de copias temporales de evaluación; no se retiraron firmas globales de E2. Evidencia y límites en `docs/audits/2026-09-07/learning-inbox-source-authority.md`.
- Evaluación automotriz: directorio transaccional aislado, consultas privadas con precondición de identidad sintética, nueve escenarios en cuatro idiomas y verificación de vehículo/términos/servicio/asesor/horario/contacto. Los ciclos del executor usan ledger y consentimiento reales, con y sin anticipo, sin OTP, calendario ni escrituras en el esquema origen. Se corrigieron moneda de evidencia de citas, procedencia de aprobación en replay y un cast SQL descubierto con DDL real. **17 suites / 218 casos pasan**, incluidos 33 PostgreSQL de comandos canónicos y 17 de vehículos; API TypeScript y shared pasan. Matriz: **76 perfiles / 268 tareas / 146 transaccionales / 40 sin positivo / 18 sin verificador / cero certificados**. Los escenarios todavía no se ejecutaron con LLM por revisión/canal/idioma. Detalle en `docs/audits/2026-09-07/vehicle-appointment-consolidation.md`.

El 7 de septiembre la revisión automática rechazó crear el siguiente commit con motivo «usage limit». Los tres subagentes también terminaron por ese límite. Se continuaron localmente las correcciones y pruebas anteriores; no se intentó eludir el rechazo ni se consumió un reset. La revisión volvió a estar disponible y los bloques indicados arriba se registraron incrementalmente; aquel rechazo no es un bloqueo vigente. El alcance completo permanece activo y no se declara finalizado.

Los números de suites anteriores se solapan: no deben sumarse como si fueran casos independientes. Los tests usan servicios reales con dependencias aisladas; no certifican proveedores externos ni resultados de mercado.

### Frentes abiertos al cierre del 8 de septiembre

- Aprendizaje: replay/juez, retiro de copias y recuperación periódica tienen evidencia en `learning-evaluation-retention.md`; el ownership del worker, en `learning-worker-ownership.md`; embeddings, búsqueda/memoria históricas y reintentos de SDK, en `learning-external-source-authority.md`. `54379f90` añade procedencia serializable y verificación sobre la misma conexión; `1f704ddc` incorpora borrado de derivados Widget y `735a0511` prepara acumulación/lectura de historia. La admisión de mensajes normales sigue sin consumidor activo: core/Gateway/store están preservados como borrador con bloqueos explícitos en el traspaso para Claude. Continúan particiones de proceso/cola, otras salidas externas y despacho diferido, retiro de todas las trazas/caches, costo/deadline integral y medición semántica de la mejora. No incluye proveedor externo ni certificación por perfil.
- Assist, editor e Inbox: fixture con datos sintéticos comprobado por HTTP; la herramienta de navegador falló al inicializar, incluso tras reset. Revisión visual, accesibilidad y prueba con usuarios nuevos permanecen pendientes. El fixture no equivale a piloto de backend/proveedores.
- Evaluación: las instancias efímeras de PostgreSQL/pgvector/Valkey están separadas de los datos y volúmenes existentes. Quince writers canónicos están habilitados exclusivamente con lease de namespace; pagos y las familias restantes siguen bloqueados. El gestor de ciclo de vida RAG y su integración con consumidores, jueces y retención de recorridos están registrados y probados; la integración quedó en `5f036d10`. El manifiesto integral conserva dependencias vivas vigiladas; el tráfico puede invalidar una evaluación larga. Siguen pendientes lectores comerciales congelados, integración del capturador piloto de servicios, reloj coherente, presupuesto/rendimiento bajo carga y cobertura de todas las tareas. Matriz: 76 perfiles, 268 tareas, 146 transaccionales, 32 sin positivo propio y 10 sin verificador; cero perfiles certificados. La identidad sintética no certifica un OTP ni la identidad de un cliente real.
- Publicación y efectos: 16 herramientas comprueban autoridad operativa, incluida la admisión durable de `create_payment_link`; las entregas aprobadas de imágenes/enlaces de Web Chat también comprueban la conexión al guardar. `9e5016f8` corrige la selección al reanudar propuestas del segundo agente y el vínculo tenant/esquema del contexto y finalización. Todavía faltan respuestas normales, otras escrituras/efectos y despacho externo, handoff, Flow, publicación HTTP, piloto, promoción y rollback integral. El guard de conexión no se ha propagado automáticamente a los dieciséis comandos. Las primitivas de almacenamiento y las pruebas locales no activan las superficies pendientes.
- Aprobaciones: entrega durable posterior a la ejecución implementada para media, enlace de pago y handoff; transporte asíncrono Web Chat con autenticación, replay y confirmación de recepción separado de persistencia probado localmente. Falta piloto de proveedores y navegador real.
- Continúan dentro del alcance: conciliación con proveedores de avisos inciertos; recuperación histórica de las familias restantes; ciclos completos, términos y positivos de las tareas pendientes; cobertura semántica de conocimiento; evidencia operacional de las familias todavía sin verificador; autoridad de versión en todos los efectos, publicación/piloto/rollback integral del agente; pruebas con usuarios; pilotos externos y comparación de desempeño. La matriz declarativa y sus huecos deben regenerarse al cambiar los packs y verificadores.

No se ha hecho push, despliegue ni migración sobre tenants existentes. La meta de ser el mejor agente requiere resultados medidos y comparación externa; no se declara conseguida por pasar pruebas locales.

## Decisiones reservadas para el cierre

Se documentarán aquí únicamente decisiones que no puedan resolverse con el plan, las reglas actuales del negocio o una implementación reversible. La ausencia de una decisión opcional no detiene los demás frentes.

### H4-01 — CONFIRMED_WITH_CORRECTION (severity: keep)
Evidence holds at HEAD: `agent-assessment.service.ts:241-308` builds tasks = mission (first, `tourId: null`) + the 7 keys of `AGENT_SETUP_TASK_CHECKS` (appointments skipped only when every check is `not_applicable`) + catalog (only when a vertical catalog exists, `tourId: null`) + tests. `InitialSetupCard.tsx:137` renders `sm:grid-cols-2 xl:grid-cols-3`. Plan §12.3 (`docs/assist-quality-guided-tours-plan-2026-09.md:593-608`) specifies 6 items, channel first, each with Continuar + Mostrarme dónde. Commits 751d23e7 (Sep 7) added mission/tests server-side; b5b71b60 (Sep 8) deleted the client 6-item definition. Two corrections: (1) count is 8-10, not 9-10 (appointments and catalog are both conditional); (2) the tests task DOES carry `tourId: 'run_agent_tests'` (line ~299; tour defined in `guided-tour-contract.ts:140`, minRole tenant_admin), so only mission and catalog lack "Mostrarme dónde". No doc/commit marks the divergence from §12.3 as a deliberate decision; §12.3 says the card is "el hub de esenciales", which the current list exceeds. Video quote is unverifiable here.
- CORRECTION: Item count is 8-10 (appointments is skipped when all its checks are not_applicable; catalog only appears for verticals with a catalog), not 9-10. The tests task does have a "Mostrarme dónde" tour (`tourId: 'run_agent_tests'`); the items without a tour are mission and catalog (both hard-coded `tourId: null`).
- BY DESIGN: No ADR, doc or commit message states that exceeding §12.3's 6 essentials was decided. §12.3 and line 476 of the same plan still describe the card as "el hub de esenciales"; commit 751d23e7 added mission/tests as part of the shared-assessment refactor without amending the plan.
- `apps/api/src/modules/copilot/agent-assessment.service.ts:241-242` const tasks: AgentSetupTask[] = [withState({ key: 'mission', … href: `/admin/agent/${agent.id}`, tourId: null, dependsOn: [] },
- `apps/api/src/modules/copilot/agent-assessment.service.ts:255-257` for (const [key, codes] of Object.entries(AGENT_SETUP_TASK_CHECKS)) { … if (key === 'appointments' && relevant.every(check => check.status === 'not_applicable')) continue;
- `apps/api/src/modules/copilot/agent-assessment.service.ts:266-269` if (catalog) { … tasks.push(withState({ key: 'catalog', … tourId: null, dependsOn: ['mission'] }));
- `apps/api/src/modules/copilot/agent-assessment.service.ts:294-299` tasks.push(withState({ key: 'tests', … href: `/admin/agent/${agent.id}/test`, tourId: 'run_agent_tests', dependsOn: ['mission', 'agent', 'knowledge'] }

### AE-02 — CONFIRMED_WITH_CORRECTION (severity: lower)
Evidence matches HEAD verbatim: `toggleChannel` (315-319) flips local state and the chip is a bare `<button aria-pressed onClick={toggleChannel}>` (869) with no label, confirm, or undo; `agentChannelAssignmentIssues` only flags unsupported/disconnected/stale, never "no channel left". The video moment is recorded in docs (transcript analysis line 220). No commit/doc shows a deliberate choice; the chip shipped as-is in 0bcbb7e7. Correction: the tap does NOT unassign anything in production — it only edits the draft; nothing persists until "Guardar" (`handleSave` 450+, `channelsToSave` 470-475), and the page even says the selections belong to the draft (i18n `draftScope`). So the harm is confusion plus a silent draft mutation that gets saved with any other change, not an immediate live unassignment. Lower to high (still an affordance defect, but reversible before save).
- CORRECTION: The chip tap only mutates local draft state; the unassignment is not persisted until the user presses Guardar (handleSave → saveAgentDraft). "Silently unassigns" should read "silently marks the channel unassigned in the draft, with no unsaved indicator, so it gets persisted with the next save." No confirmation exists for removing the last channel either in the toggle or in handleSave.
- `apps/dashboard/src/app/admin/agent/[agentId]/page.tsx:315-319` function toggleChannel(channel: string) { setAssignedChannels(prev => prev.includes(channel) ? prev.filter(c => c !== channel) : [...prev, channel]); }
- `apps/dashboard/src/app/admin/agent/[agentId]/page.tsx:869` <button key={ch} type="button" aria-pressed={isAssigned} onClick={() => toggleChannel(ch)} className={chipCls(isAssigned)}>
- `apps/dashboard/src/app/admin/agent/[agentId]/page.tsx:470-484` const channelsToSave = Array.from(new Set([...assignedChannels.filter(t => !multiAccountTypes.has(t)), ...foldedTypes])); ... await api.saveAgentDraft(activeTenantId, agentId, saveAttempt.current.requ
- `apps/dashboard/src/lib/agent-channel-assignment.ts:68` reason: 'unsupported' | 'disconnected' | 'stale';

### W2-01 — CONFIRMED_WITH_CORRECTION (severity: keep)
Evidence holds at HEAD: setup-wizard/page.tsx:501-507 mounts AgentDraftStatus (when workspace), AgentAssessmentPanel and HelpPanel unconditionally above the stepper (line 521), so every step shows them. AgentAssessmentPanel.tsx:65-83 renders title, operational-state summary, "assessmentOperational" note, mission text, source, guidance, mission editor, next/askAssist and a details block. Spanish strings confirmed: "Versión operativa y borrador", "Borrador guardado", "publicarla", and the pending verdict "El agente todavía no está listo para atender…". git log -S confirms d50d1670 (AgentDraftStatus) and 751d23e7 (AgentAssessmentPanel), both 2026-09-07. Correction: 4f2390e4 did not remove such layers from the wizard; it ADDED the HelpPanel there (docs/assist plan §505 called the wizard's missing HelpPanel a gap). The "~186 words" count is unverifiable statically (depends on assessment data). Two internal docs (untracked, onboarding-ux-benchmark-and-video-audit.md:93,216; video-transcript-analysis.md:58) independently flag the same clutter; no doc or commit shows it was a deliberate wizard decision.
- CORRECTION: 4f2390e4 (Sept plan) did not remove these layers from the wizard; it introduced the HelpPanel there deliberately (docs/assist-quality-guided-tours-plan-2026-09.md:505 listed the wizard's missing HelpPanel as a gap). The two assessment/draft panels were added three days later by d50d1670 and 751d23e7. The "~186 words" figure cannot be verified statically; it varies with assessment data.
- BY DESIGN: HelpPanel in the wizard was a deliberate outcome of the Sept plan (docs/assist-quality-guided-tours-plan-2026-09.md:505 "setup-wizard sin HelpPanel" as a hole). No doc/commit message frames placing AgentDraftStatus/AgentAssessmentPanel above the wizard stepper as a decision; AgentAssessmentPanel.tsx:16 states "Every configuration surface uses this same server assessment" as the rationale for mounting it everywhere.
- `apps/dashboard/src/app/admin/setup-wizard/page.tsx:501-507` {workspace && <AgentDraftStatus workspace={workspace} tenantId={tenantId} />}
<AgentAssessmentPanel />
<HelpPanel
- `apps/dashboard/src/app/admin/setup-wizard/page.tsx:520-523` {/* Progress */}
<div id={guidedTourAnchorId("setup-steps")} className="mb-6">
- `apps/dashboard/src/components/quality/AgentAssessmentPanel.tsx:65-83` <AgentOperationalStateSummary state={assessment.state} className="mt-2" /> … <AgentMissionEditor … /> … {t('next')} … {t('askAssist')} … <details
- `apps/dashboard/messages/es.json:333,349,11995` "title": "Versión operativa y borrador" … "Esta evaluación describe la versión operativa. Prueba el borrador…" … "pending": "El agente todavía no está listo para atender: queda configuración por hacer

### EF-1 — CONFIRMED_WITH_CORRECTION (severity: lower)
All cited evidence exists at HEAD as quoted: `createGenericVertical('otro', {})` (line 1299) inherits agent name 'Asistente', `services: []`, `bookingEnabled: false`, and five FAQs that invite the customer to ask rather than answer (e.g. location → "Escríbenos y te la confirmamos"). Persona template `tpl_otro_ventas` at persona.service.ts:2860-2882 matches. Two corrections: (1) the deflecting FAQ wording is deliberate — commit 95f758f3 replaced invented policy claims with "invite to confirm instead of inventing" because seeded facts hallucinated; the defect is that they are seeded at all for a business that gave no data, not the wording. (2) "nothing about the business" overstates it: onboarding `chatReasons`/`customerTypes` do reach the prompt via `vertical-turn-context.service.ts:174-177` (businessGoals/targetAudiences), so the agent knows goals/audiences, though still no services, prices, location or booking. Docs already record the gap (vertical-maturity-audit-2026-07.md:183; cluster-resto.json:108).
- CORRECTION: The FAQ "deflection" wording was a deliberate fix (commit 95f758f3: FAQs "invitan a confirmar en vez de inventar el dato") to stop hallucinated policies; the real defect is seeding placeholder FAQs with no owner input. Also, the generic tenant is not fully blank: onboarding chatReasons/customerTypes are injected into the turn context as businessGoals/targetAudiences. Services, prices, location and booking remain absent.
- BY DESIGN: Commit 95f758f3 ("que la adaptacion por industria llegue de verdad al runtime"): "Neutralizadas las afirmaciones de politica que el tenant nunca confirmo... Ahora invitan a confirmar en vez de inventar el dato." The generic-vertical gap itself is documented as open in docs/vertical-maturity-audit-2026-07.md:183 and docs/vertical-audit-workdir/cluster-resto.json:106-119, not as a closed decision.
- `apps/api/src/modules/verticals/vertical-definitions.ts:1299` const OTRO = createGenericVertical('otro', {});
- `apps/api/src/modules/verticals/vertical-definitions.ts:530, 546, 555` agent: { name: { es: 'Asistente', ... } ... services: [], ... bookingEnabled: false,
- `apps/api/src/modules/verticals/vertical-definitions.ts:543` '¿Dónde están ubicados?' ... 'Con gusto te compartimos nuestra dirección y cómo llegar. Escríbenos y te la confirmamos.'
- `apps/api/src/modules/persona/persona.service.ts:2860-2882` id: 'tpl_otro_ventas' ... name: 'Asistente', role: 'Asistente comercial' ... forbiddenTopics: ['Precios no confirmados', 'Promesas de entrega sin verificar', ...]

### EF-3 — CONFIRMED_WITH_CORRECTION (severity: keep)
All three evidence points exist at HEAD. The /onboarding submit payload (page.tsx:765-782) carries only name/website/phone/email/about/socialMedia/industry/subType/orgSize/timezone/country; grep finds no address, services, prices or hours fields anywhere in that page. The prompt assembler reads b.about/phone/website/address at :265-269 (line range in finding was 260-269, slightly off) and services only through <available_services> from the services table. The education vertical seeds deflecting FAQs (:503 "Los costos varían según el programa…") and hard-coded hours (:513). The post-onboarding setup wizard at HEAD does not ask them either: its ToolsTour only links out to /admin/knowledge/faqs, /admin/service-catalog, etc. Deliberate: the Sep-2026 plan (docs/assist-quality-guided-tours-plan-2026-09.md:604-659) chose a "tarjeta de puesta en marcha" that routes the owner to business_hours/appointments_setup/knowledge_base screens rather than collecting the answers inline, and the vertical FAQs deliberately deflect instead of inventing prices. That design intent confirms rather than negates the day-0 gap.
- CORRECTION: Prompt-assembler citation is lines 265-269 (not 260-269); onboarding payload is lines 765-782. The post-onboarding setup wizard (ToolsTour) also does not collect these answers — it only links to the four screens — so "4 different screens" holds even after the Sep-2026 onboarding redesign.
- BY DESIGN: docs/assist-quality-guided-tours-plan-2026-09.md §puesta en marcha (lines 604-659) deliberately routes the owner to separate screens (business_hours, appointments_setup, knowledge_base) via a startup card and guided tours instead of asking in the wizard; vertical FAQ seeds deliberately deflect on price/schedule rather than inventing values.
- `apps/dashboard/src/app/onboarding/page.tsx:765-782` company: { name: companyName, website: normalizeWebsite(website) || undefined, phone: phone || undefined, email: businessEmail.trim() || undefined, about: about || undefined, socialMedia: {...}, indus
- `apps/api/src/modules/conversations/prompt-assembler.service.ts:265-269` if (b.about) lines.push(`    <about>...`); if (b.phone) ...; if (b.website) ...; if (b.address) lines.push(`    <address>${this.xmlEscape(b.address)}</address>`);
- `apps/api/src/modules/verticals/vertical-definitions.ts:503,513` { question: { es: '¿Cuánto cuesta?' ... }, answer: { es: 'Los costos varían según el programa. Cuéntame cuál te interesa...' } } ... businessHours: { schedule: { mon: '07:00-20:00', ... sat: '08:00-14
- `apps/dashboard/src/app/admin/setup-wizard/_components/ToolsTour.tsx:42,77` { key: "faqs", icon: HelpCircle, href: "/admin/knowledge/faqs" } ... serviceCatalog: { key: "serviceCatalog", icon: Tags, href: "/admin/service-catalog", brief: true }

### W2-03 — CONFIRMED_WITH_CORRECTION (severity: keep)
All three quotes exist at HEAD as cited. Wizard `saveAgentEdits` (page.tsx:328-339) writes name+greeting via `api.saveAgentDraft` to a draft revision; `finish` (440-463) only dispatches the `publish_agent_revision` tour (guided-tours.ts:173-179, 5 stops). `agent-draft.service.ts:75-76` explicitly refuses activation in a draft ("Reactivation belongs to publication"), and `agent-release-contract.ts:55-66` gates review on channels×languages samples. This is a deliberate owner decision: commits d50d1670 (2026-09-07, "separate editable drafts and reviewed release candidates") and cb9088cb (2026-09-13, "lead saved drafts into reviewed publication"). Correction: a direct-to-operational path still exists — `POST :tenantId/setup-wizard` (persona.controller.ts:198-420) merges `customizations.agentName/greeting` into the live default agent — but the dashboard only uses it when the tenant has no agent (`hasAgentRef` false, page.tsx:342-358); bootstrapped day-0 tenants take the draft path. "6 review checkboxes" not verified.
- CORRECTION: The wizard is not purely draft-only: the server endpoint `POST :tenantId/setup-wizard` still applies agentName/greeting straight onto the live default agent, but the page only takes that branch when the tenant has no agent (`hasAgentRef` false). Tenants bootstrapped with a vertical agent (the normal day-0 case) go through the draft + 5-stop tour. The "6 review checkboxes" detail was not verified in the cited files.
- BY DESIGN: Commit d50d1670 (2026-09-07) "feat(configuration): separate editable drafts and reviewed release candidates" and cb9088cb (2026-09-13) "fix(onboarding): lead saved drafts into reviewed publication"; code comment in agent-draft.service.ts:75 "Reactivation belongs to publication" and page.tsx:457-458 "Finishing this wizard prioritizes the review that makes the saved draft live."
- `apps/dashboard/src/app/admin/setup-wizard/page.tsx:335-339` const result = await api.saveAgentDraft(tenantId, current.agentId, saveAttempt.current.request); … return advanceStage(options);
- `apps/dashboard/src/app/admin/setup-wizard/page.tsx:452-459` detail: { tourId: "publish_agent_revision", agentId: workspaceRef.current.agentId } … // Finishing this wizard prioritizes the review that makes the saved draft live.
- `apps/api/src/modules/persona/agent-draft.service.ts:75-76` // Deactivation is an immediate safety action. Reactivation belongs to publication. if (body.isActive !== operational.is_active) throw new BadRequestException({ error: 'agent_activation_managed_separa
- `apps/api/src/modules/simulation/agent-release-contract.ts:55-66` const expectedSamples=(candidate.channels?.length||0)*(snapshot?.releaseScope?.languages?.length||0); … eligibleForReview:complete&&readiness.eligibleForReview&&samples.length===expectedSamples&&expec

### R6-02 — CONFIRMED_WITH_CORRECTION (severity: lower)
Substance holds at HEAD. `AgentAssessmentService.getAssessment` reads `agent_personas` (config_json, version) and `AgentQualityService.loadAgent` does the same; neither consults the revision store (no `draft`/`revisionId` reference in agent-quality.service.ts). The setup-wizard renders `<AgentAssessmentPanel />` (setup-wizard/page.tsx:502) while the wizard edits `workspace.draft?.body`, so the readiness verdict on screen grades the operative version, not the draft. Corrections: (1) the Spanish text is not a literal in the TSX; line 71 renders `tDraft('assessmentOperational')`, with the string in `messages/es.json:349`. (2) The wizard does offer "Prueba el borrador" (lab evaluates the draft revision), so the draft is testable, just not scored by the readiness panel. Deliberate design: `docs/audits/2026-09-07/agent-configuration-revisions.md` (commit d50d1670) states Assist's health evaluation "sigue siendo de la versión operativa y se identifica así"; the 2026-09-14 readiness audit calls the separation "legítima" but flags the unclear scope banner.
- CORRECTION: The quoted Spanish sentence lives in apps/dashboard/messages/es.json:349 (key `assessmentOperational`), not as a literal at AgentAssessmentPanel.tsx:71, which renders it via `tDraft('assessmentOperational')`. Also, a draft-testing path ("Prueba el borrador" / lab with revision UUID) exists; the gap is specifically that the readiness/assessment panel shown inside the wizard never scores the draft.
- BY DESIGN: docs/audits/2026-09-07/agent-configuration-revisions.md (commit d50d1670 "separate editable drafts and reviewed release candidates"): "Su evaluación de salud sigue siendo de la versión operativa y se identifica así." docs/audits/2026-09-14/assistant-readiness-validation.md:85 calls the separation "legítima" but notes the banner does not clarify scope next to draft selections.
- `apps/api/src/modules/copilot/agent-assessment.service.ts:175-178` SELECT id, name, template_id, version, config_json, channels, channel_bindings FROM agent_personas
- `apps/api/src/modules/quality/agent-quality.service.ts:243-252` SELECT id, name, is_active, config_json, channels, channel_bindings, version, updated_at FROM agent_personas WHERE id = $1::uuid
- `apps/dashboard/src/components/quality/AgentAssessmentPanel.tsx:71` <p className="mt-2 text-xs text-neutral-500">{tDraft('assessmentOperational')}</p>
- `apps/dashboard/messages/es.json:349` "assessmentOperational": "Esta evaluación describe la versión operativa. Prueba el borrador para evaluar sus cambios."

### L5-01 — CONFIRMED_WITH_CORRECTION (severity: keep)
All three quotes exist at HEAD as cited. Coexistence is `recommended: true` and the page pre-selects it; migration is the third card and its only "diagnosis" of an existing BSP is two warning strings shown after the user already chose migration (routeMigWarnBM mentions "tu proveedor actual"). The pre-check is three generic checkboxes with no branching. No triage question anywhere in the folder. Corrections: (1) the 2FA-off is a warning text, not a code-enforced requirement; (2) the fix hint's "Meta-free test" alternative was deliberately removed — the component comment says that route "no existe más en el producto"; (3) the soft gate and coexistence-as-star are deliberate owner decisions (code comment; onboarding redesign doc), which does not remove the gap but must be reported. Video timings/ESU failures are not verifiable from code.
- CORRECTION: 2FA-off is a warning string (routeMigWarn2FA), not a hard gate. The suggested "Meta-free test" alternative was intentionally removed from the product (component comment lines 15-16), so the fix hint must not rely on it. Video-derived impact is unverifiable from code.
- BY DESIGN: WhatsAppPrerequisites.tsx:13 states the soft gate is intentional; docs/onboarding-redesign-2026-q2.md:20 sets Coexistence as the star route; commit 4f2390e4 introduced the current route ordering.
- `apps/dashboard/src/app/admin/channels/whatsapp/whatsapp-connect-routes.ts:42-75` id: "coexistence", ... recommended: true, ... id: "migration", ... warningKeys: ["routeMigWarnBM", "routeMigWarn2FA"]
- `apps/dashboard/src/app/admin/channels/whatsapp/page.tsx:69` useState<WhatsAppConnectRouteId>("coexistence")
- `apps/dashboard/src/app/admin/channels/whatsapp/WhatsAppPrerequisites.tsx:13-16` Es un gate suave (awareness), no validación dura. ... La nota que ofrecía "probá con un número de prueba" salió: esa ruta no existe más en el producto
- `apps/dashboard/messages/es.json:4004-4005` routeMigWarn2FA: La desactivación del 2FA depende de tu proveedor actual y puede tardar entre minutos y 24 horas

### W2-02 — CONFIRMED_WITH_CORRECTION (severity: keep)
Evidence holds at HEAD. No bootstrap/vertical code seeds `config_json.mission` (grep in verticals/, onboarding/, persona/ finds no writer), so `saved === undefined` → `configured` false → mission task `warning` with explicit override `'pending'` (lines 241-245). `rollUpOperationalState` picks the least-advanced known state, and `pending` sits at the bottom of the ladder, so the whole agent reads `pending` → es.json "El agente todavía no está listo para atender". Correction: the backend gate is NOT the three textareas — `configured` requires only `objective` + non-empty `intentKeys` (line 212); the three `required` textareas are a client-side constraint of AgentMissionEditor.tsx (lines 63/70/71), which in practice forces all three to save. Deliberate: commit 64e6e936 states `warning` on mission means "running on the template's mission", chosen as `pending` precisely so a new agent isn't called degraded — the owner intentionally counts template mission as unfinished setup, not as satisfied.
- CORRECTION: Readiness gate is `saved.version === 1 && text(saved.objective) && strings(saved.intentKeys).length > 0` (agent-assessment.service.ts:212) — objective + intents, not success criteria/handoff. The three-textarea requirement lives only in the dashboard editor form (AgentMissionEditor.tsx lines 63, 70, 71). Roll-up is at line 377-383, not 376-382.
- BY DESIGN: Commit 64e6e936 "feat(copilot): make the shared assessment carry the shared word": "`warning` on the mission means 'running on the template's mission', while everywhere else warning means 'revisar'. Deriving it would have called a brand new agent degraded." Code comment lines 243-244: "unfinished setup, not a regression". The owner chose `pending` (not `degraded`) but deliberately did not count the template mission as `pass`.
- `apps/api/src/modules/copilot/agent-assessment.service.ts:211-212` const saved = config.mission;
        const configured = saved?.version === 1 && text(saved.objective) && strings(saved.intentKeys).length > 0;
- `apps/api/src/modules/copilot/agent-assessment.service.ts:241-245` withState({ key: 'mission', status: ... configured ? 'pass' : 'warning', ... // `warning` on this task means "running on the template's mission" ... configured ? undefined : 'pending')
- `apps/api/src/modules/copilot/agent-assessment.service.ts:377-383` state: rollUpOperationalState([ operationalStateFromQuality(overview.status), ...tasks.map(task => task.state), ...
- `packages/shared/src/agent-operational-state.ts:142-150` return known.reduce((worst, state) => PROGRESS[state] < PROGRESS[worst] ? state : worst, known[0]);

### R6-01 — CONFIRMED_WITH_CORRECTION (severity: lower)
All cited lines exist at HEAD as quoted. But the finding is partly stale: commits 4c6516a5 and 64e6e936 (2026-09-08) already name the "six vocabularies" problem and add one shared word (unknown/pending/prepared/tested/operating/degraded) rolled up server-side into `assessment.state`; `OperationalState.tsx` is shared so the assessment panel and quality centre render the same field. The fix hint is thus already the adopted direction. What actually contradicts on the editor screen is different from what the finding says: "Versión operativa 5 · Atendiendo" and "Está respondiendo a tus clientes" come from `workspace.operational.body.isActive` (the activation flag), not from any readiness computation, while "Pendiente… no está listo" comes from `assessment.state` on the same page. Quality banners (`worstStatus`, `attentionCount`) keep their own words by design ("a projection, not a rename"). Whether all five texts appear on one screen at 00:48 needs the video/runtime.
- CORRECTION: Not six independent readiness computations disagreeing: since 2026-09-08 the assessment rolls quality+tasks+channels+tools+tests into one `state`. The remaining day-0 contradiction is the activation flag (`isActive` → "Atendiendo"/"Está respondiendo") shown beside `assessment.state === 'pending'` on `/admin/agent/[agentId]`, plus quality banners still using `worstStatus`/`attentionCount` vocabulary.
- BY DESIGN: git commit 4c6516a5 (2026-09-08) "give every surface one vocabulary for the state of an agent": "Six vocabularies were in use at once… This is a projection, not a rename: every vocabulary keeps its meaning and gains one total mapping onto unknown / pending / prepared / tested / operating / degraded". Commit 64e6e936 makes the shared assessment carry that word. The owner deliberately kept the underlying vocabularies and added a shared projection instead of collapsing them.
- `apps/api/src/modules/copilot/agent-assessment.service.ts:377-382` state: rollUpOperationalState([operationalStateFromQuality(overview.status), ...tasks.map(task => task.state), ...statedChannels.map(channel => channel.state), ...tools.map(tool => tool.state), ...req
- `apps/dashboard/src/components/quality/AgentDraftStatus.tsx:32` {t('operationalVersion', { version: workspace.operational.version })} · {t(workspace.operational.body.isActive ? 'operationalActive' : 'operationalInactive')}
- `apps/dashboard/src/app/admin/agent/[agentId]/page.tsx:623-624, 776` <AgentDraftStatus workspace={workspace} … /> <AgentAssessmentPanel agentId={agentId} /> … {isActive ? t("activation.activeHint") : t("activation.inactiveHint")}
- `apps/dashboard/src/components/quality/OperationalState.tsx:76-77` Shared by the assessment panel and the quality centre so that the two surfaces cannot disagree: both render this from `assessment.state`

### EF-5 — CONFIRMED_WITH_CORRECTION (severity: keep)
Evidence holds at HEAD. `createDefaultAgentFromGoals` (persona.service.ts:3122-3133) disables the appointments tool with `pendingPrerequisites:true` when the fresh schema has 0 services or 0 slots. For 'otro', `createGenericVertical` yields `bookingEnabled:false, services:[]` (vertical-definitions.ts:546,555,1299), goals do not override it (`agendaAllowed = definition.bookingEnabled && ...`, verticals.service.ts:485), so `seedAvailability` is skipped and `restoreAppointmentsTool(…, effectiveBooking=false)` takes the disable branch (2724-2747). Correction: the marker does NOT survive — it is deleted and the tool ends as plain `enabled:false`; the disabling for 'otro' is capability-driven, not just prerequisite-driven. The quality center's `tool_appointments` check exists (agent-quality.service.ts:980) but `optionalToolStatus` returns `not_applicable` whenever `enabled !== true` (1313-1316), so no health item fires. Deliberate: code comment 2698-2701 states the owner can re-enable from Agente → Herramientas; no notification surface was designed.
- CORRECTION: After bootstrap the `pendingPrerequisites` marker is removed by `restoreAppointmentsTool`; the agent ends with `tools.appointments.enabled:false` and no marker. For 'otro' the root cause is `createGenericVertical` → `bookingEnabled:false` (capability authoritative), which also skips `seedAvailability`, not only the empty-schema gate. The existing `tool_appointments` quality check is silent because it reports `not_applicable` for disabled tools.
- BY DESIGN: verticals.service.ts:2698-2701 comment: "El marcador se borra siempre al evaluarlo — si la siembra no alcanzó, la herramienta queda apagada de forma limpia y el tenant puede encenderla desde Agente → Herramientas cuando complete su agenda". Introduced in commit 95f758f3 (fix(verticals): que la adaptacion por industria llegue de verdad al runtime). Deliberate silent-off; no owner-facing prompt was specified.
- `apps/api/src/modules/persona/persona.service.ts:3122-3131` if (configJson?.tools?.appointments?.enabled === true) { const { services, slots } = await this.countAppointmentsPrerequisites(schemaName); if (services === 0 || slots === 0) { … appointments: { ...co
- `apps/api/src/modules/verticals/vertical-definitions.ts:525-555, 1299` function createGenericVertical(…) { … services: [], … bookingEnabled: false … } / const OTRO = createGenericVertical('otro', {});
- `apps/api/src/modules/verticals/verticals.service.ts:485, 856-865, 2724-2747` agendaAllowed: definition.bookingEnabled && !bootstrapMode?.skipAgenda … if (effectiveBooking) { await this.seedAvailability(…) } await this.restoreAppointmentsTool(schemaName, effectiveBooking, query
- `apps/api/src/modules/quality/agent-quality.service.ts:980, 1313-1316` add({ code: 'tool_appointments', … status: this.optionalToolStatus(tools.appointments, facts.services > 0 && facts.availabilitySlots > 0) … }) / private optionalToolStatus(tool, ready) { if (tool?.ena

### EF-2 — REFUTED (severity: lower)
The quoted code exists (orFallback keeps any non-empty name; tpl_otro_ventas/soporte carry 'Asistente'), but the collision never happens on day 0. The onboarding resolver maps every industry that has a registry agent name (salud→Sofía, moda_belleza→Luna, inmobiliaria→Carlos, restaurantes→Luca, automotriz→Marco, servicios_hogar→Diego, pet_services→Toby, fotografia→Camila, seguros→Roberto, gimnasios→Alex, veterinaria→Dra. Ana) to a dedicated vertical template whose persona.name already is that human name (e.g. tpl_salud_recepcion 'Sofía', tpl_restaurante 'Luca'). The only industries resolving to the 'Asistente' template (event_planning, construccion, otro) have NO agent.name in vertical-definitions.ts, so pick(agentDef.name) is '' and "treat the placeholder as empty" would change nothing. buildDefaultPersona's 'Asistente' sits under the template in deepMergeConfig, so it only survives with an empty template name. Fill-gaps is a documented owner decision (commit 95f758f3: "RELLENA HUECOS, no pisa"). Residual truth: those three industries do show "Preparamos a Asistente", but the cause is a missing registry name, not orFallback.
- CORRECTION: If kept as a finding, restate it as: nine industries (event_planning, construccion, otro, turismo, education, finanzas, servicios_profesionales, retail, technology) define no agent.name in vertical-definitions.ts, and three of them resolve to tpl_otro_ventas whose persona is the generic 'Asistente'; the fix is adding registry/template names, not changing orFallback.
- BY DESIGN: Commit 95f758f3 fix(verticals): "Decision: RELLENA HUECOS, no pisa — la plantilla vertical es la fuente real y esta mejor escrita, y este metodo tambien corre en re-seed sobre tenants vivos, donde pisar borraria lo que el dueño edito."
- `apps/api/src/modules/verticals/verticals.service.ts:1923, 1998-1999` name: this.orFallback(existingPersona.name, pick(agentDef.name)) … return typeof current === 'string' && current.trim().length > 0 ? current : fallback;
- `apps/api/src/modules/persona/onboarding-persona-resolver.ts:~236-258` salud: defaultTemplateId: 'tpl_salud_recepcion' … event_planning / construccion / otro: defaultTemplateId: 'tpl_otro_ventas'
- `apps/api/src/modules/verticals/vertical-definitions.ts:30-49, 1213-1228` industry: 'salud' … name: { es: 'Sofía' } … industry: 'veterinaria' … name: { es: 'Dra. Ana' } (no agent.name under event_planning, construccion, otro, turismo, education, finanzas, servicios_profesio
- `apps/api/src/modules/persona/persona.service.ts:1565-1571, 1686-1692, 2860-2867, 3108` 'Sofía - Recepción Médica' … name: 'Sofía' … 'Luca - Reservas y Menú' … name: 'Luca' … id: 'tpl_otro_ventas' … name: 'Asistente' … deepMergeConfig(this.buildDefaultPersona(tenantId), template.config_j

### AE-01 — CONFIRMED (severity: keep)
Evidence holds at HEAD. Line 1041/1043 pick colour and icon solely by `toast.includes("Error") || toast.includes("error")`; every other string renders `bg-emerald-500` + `CheckCircle`. `setToast(t("validation.blocked"))` fires at lines 459 and 507 inside `handleSave`; `t` is `useTranslations("agent")` and es.json 4705 sits under `agent.validation.blocked` ("Faltan datos obligatorios…"), which contains neither substring, so the failure shows green with a check. Same in en/pt/fr. Scope is wider than the finding states: `promptRequired`, `activationReview`, `editorChanged`, `templateOperationalOnly`, `defaultNeedsSave` and `channelOverviewUnavailableHint` are all blocking/refusal toasts rendered green too. No doc, comment or commit justifies the heuristic: the substring test dates from 0bcbb7e7 (multi-agent), and the blocked message was added later in 4f2390e4 without adapting the toast, so it is drift, not a deliberate decision.
- CORRECTION: Scope is broader than one string: the same green rendering applies to all non-success toasts on this page (agentDraft.promptRequired, activationReview, templateOperationalOnly, defaultNeedsSave; agentConfiguration.editorChanged; agent.channelOverviewUnavailableHint). Only strings containing "Error"/"error" (mostly common.errorSaving and backend error bodies) go red.
- BY DESIGN: None found. The substring heuristic comes from commit 0bcbb7e7 (multi-agent system); validation.blocked was introduced in 4f2390e4 without touching the toast tone logic. No docs/ or code comment references the behaviour.
- `apps/dashboard/src/app/admin/agent/[agentId]/page.tsx:1041-1043` toast.includes("Error") || toast.includes("error") ? "bg-red-500" : "bg-emerald-500" … ? <AlertTriangle size={16} /> : <CheckCircle size={16} />
- `apps/dashboard/src/app/admin/agent/[agentId]/page.tsx:459,507` setToast(t("validation.blocked"));
- `apps/dashboard/src/app/admin/agent/[agentId]/page.tsx:129` const t = useTranslations("agent");
- `apps/dashboard/messages/es.json:4705` "blocked": "Faltan datos obligatorios para guardar. Revisa los campos marcados en rojo."

### H4-03 — CONFIRMED_WITH_CORRECTION (severity: lower)
Evidence verified at HEAD: InstallPrompt.tsx lines 52, 91, 96-100 match the quote; the only gates are standalone mode, `beforeinstallprompt` having fired, and a localStorage snooze written on dismissal (30d after X, 7d after declining the OS dialog — commit 1bd50886). No onboarding-stage, visit-count or route gate exists. Correction on scope: the component is mounted once, in `app/admin/layout.tsx:241`, so it does NOT overlay the `/onboarding` trial-first wizard (`app/onboarding/page.tsx` lives outside the admin layout). It does overlay every `/admin/*` page, including `/admin/setup-wizard`, the setup card, tours and the help assistant. "Every frame" is only true while the owner never clicks X; after one dismissal it stays hidden 30 days. Docs only list "install prompt (A2HS)" as a deliverable; no ADR/comment justifies showing it on day 0. Fix hint (`landing === normal`) would need to be applied in the admin layout.
- CORRECTION: Overlay scope is /admin/* only (mounted at apps/dashboard/src/app/admin/layout.tsx:241), not the /onboarding wizard. A dismissal snooze exists (30d/7d, localStorage), so it is not literally persistent once closed; the missing piece is a stage/visit gate before first display.
- BY DESIGN: Commit 1bd50886 "fix(pwa): el aviso 'Instalar Parallly' reaparecía en cada carga al cerrarlo" deliberately chose a post-dismiss snooze rather than a first-visit/onboarding gate; docs/implementation-plan-2026-q2.md:214 lists the A2HS install prompt as a wanted deliverable with no display-timing rule.
- `apps/dashboard/src/components/pwa/InstallPrompt.tsx:43-52` if (Date.now() < snoozedUntil()) { setDismissed(true); } … window.addEventListener("beforeinstallprompt", handler);
- `apps/dashboard/src/components/pwa/InstallPrompt.tsx:91-100` if (isStandalone || !canInstall || dismissed) return null; … position: "fixed", bottom: 20, … zIndex: 9999
- `apps/dashboard/src/app/admin/layout.tsx:241` <InstallPrompt />
- `apps/dashboard/src/components/pwa/InstallPrompt.tsx:9-11` const SNOOZE_KEY = "pwa-install-snooze-until"; const DISMISS_SNOOZE_MS = 30 * 24 * 60 * 60 * 1000; const SOFT_SNOOZE_MS = 7 * 24 * 60 * 60 * 1000;

### AE-07 — CONFIRMED_WITH_CORRECTION (severity: lower)
Evidence holds at HEAD. verticals.service.ts:1921-1968 builds `persona` and `behavior` (rules, forbiddenTopics, handoffTriggers) and never touches `mainInstructions`; no code under verticals/onboarding/tenants writes that key at all, and no builtin template in persona.service.ts seeds it (only readers at :259 and :640). The dashboard textarea (BehaviorSection.tsx:85) binds `config.behavior.mainInstructions || ""`, so a derived agent lands with it empty while es.json:6710 says "Preparamos a {name} con lo que nos contaste". Correction of substance: the agent is NOT behaviourally unguided — the vertical patch deliberately merges industry `rules` (comment: "Se UNEN, no se rellenan… las del registry son las que instruyen las herramientas") and the prompt assembler emits them (persona.service.ts:264) alongside `main_instructions`. So the gap is a UX/perception one (blank "principal" field despite a filled rules list), not a missing-instructions defect; severity should drop. No doc/commit shows the blank field is intentional. Video timing is unverifiable here.
- CORRECTION: mainInstructions is indeed never seeded, but the derived agent is not instruction-less: the vertical patch merges registry `rules` into behavior.rules and the prompt assembler emits them. The finding is about a visibly empty "principal" field, not an unguided agent.
- BY DESIGN: The verticals.service.ts comment block (lines ~1936-1950) shows the owner deliberately chose `rules` as the vehicle for vertical guidance ("Se UNEN, no se rellenan"); nothing documents leaving mainInstructions blank as a decision.
- `apps/api/src/modules/verticals/verticals.service.ts:1933-1967` const behavior = { ...existingBehavior, rules: reconcileVerticalSubtypePersonaRules({...}), forbiddenTopics: ..., handoffTriggers: ... };
- `apps/api/src/modules/persona/persona.service.ts:259-266` if (typeof behavior?.mainInstructions === 'string' && behavior.mainInstructions.trim()) {...} ... if (behavior?.rules?.length > 0) {
- `apps/dashboard/src/app/admin/agent/_components/BehaviorSection.tsx:85` value={config.behavior.mainInstructions || ""}
- `apps/dashboard/messages/es.json:6710` "prepared": "Preparamos a {name} con lo que nos contaste de tu negocio..."

### H4-02 — CONFIRMED_WITH_CORRECTION (severity: lower)
Evidence holds at HEAD. `admin/page.tsx:441-451` silences only AgentHealthCard, the empty-state hero, QualityAttentionBanner and HelpAssistant (via `publishOnboardingLanding`); the Stats Grid (`:707-708`) and `HelpPanel` (`:536-541`) are unconditional, and `layout.tsx:207,241` render TrialCountdownBanner and InstallPrompt with no knowledge of the landing. Corrections: (1) HelpPanel is collapsed by default (`help-panel.tsx:86-88`, `defaultOpen=false`), so it is a one-line strip, not "5 tips"; (2) InstallPrompt only renders when the browser fires `beforeinstallprompt` and it is not snoozed (`InstallPrompt.tsx:91`), so it is environment-dependent; (3) the amber banner + setup card is exactly what the plan's own rule P2 allows ("como máximo un aviso y una tarjeta"). The realistic count is 5-7 surfaces, not 8-10. Substance stands: KPI tiles, trial banner and PWA prompt were never in the rule's scope.
- CORRECTION: Count is 5-7, not 8-10: HelpPanel renders collapsed (defaultOpen=false) and InstallPrompt appears only if the browser offers install and it is not snoozed. Amber banner + setup card are the plan's sanctioned "un aviso y una tarjeta"; the unsilenced extras are the 4 KPI tiles, trial banner, collapsed HelpPanel strip and (conditionally) PWA prompt.
- BY DESIGN: Partly. docs/assist-quality-guided-tours-plan-2026-09.md P2 (line 405) defines the rule as "at most one notice and one progress card" of onboarding guidance, and §11 #1 (line 514) scoped it to tour/quality banner/hero/AgentHealthCard. KPI tiles, trial banner, HelpPanel and PWA prompt were never classified as guides, so leaving them was an unaddressed scope gap rather than an explicit decision to keep them.
- `apps/dashboard/src/app/admin/page.tsx:441-451` const guideSilent = guideOwnsHome && !guideKnown; const setupCardOnly = ... guide.landing === "setup_card_only"; // el aviso rojo de calidad y la burbuja del asistente se callan
- `apps/dashboard/src/app/admin/page.tsx:536-551` <HelpPanel title={tHelp("dashboard.title")} ... /> {canViewAgentHealth && !setupCardOnly && !guideSilent && <AgentHealthCard />} {canViewAgentHealth && !guideSilent && (<InitialSetupCard
- `apps/dashboard/src/app/admin/page.tsx:707-709` {/* Stats Grid */} <div className="mb-8 grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4 animate-stagger"> {statConfig.map(
- `apps/dashboard/src/app/admin/layout.tsx:207-210, 241-244` <TrialCountdownBanner restriction={restriction} /> <EmailVerificationBanner /> <FiscalBanner /> <QualityAttentionBanner /> ... <InstallPrompt /> ... <HelpAssistant />

### AE-05 — CONFIRMED (severity: keep)
Both quoted lines exist verbatim at HEAD (page.tsx:422 and :763). The switch only opens the deactivate confirm when active; when inactive it toasts tDraft('activationReview') ("La reactivación requiere revisar y publicar una versión.") with no link or action. The backend enforces the same: PUT /agents/:id rejects any body where isActive !== false (persona.controller.ts:809, error agent_draft_contract_required) and the draft service throws agent_activation_managed_separately when isActive differs from is_active (agent-draft.service.ts:75-76, comment "Reactivation belongs to publication"). This is a deliberate owner/design decision from commit d50d1670 ("separate editable drafts and reviewed release candidates"). However, no code in the draft/copilot/revision modules was found that sets agent_personas.is_active = true on publication, so the reactivation path the toast points to is unlocated from this page; a one-way switch with a dead-end toast remains a real trap for a novice. The fix hint (link/button to the publish flow) is reasonable.
- BY DESIGN: Commit d50d1670 "feat(configuration): separate editable drafts and reviewed release candidates" introduced the split; code comment in agent-draft.service.ts:75 states "Deactivation is an immediate safety action. Reactivation belongs to publication." The asymmetry is deliberate; the missing link/CTA from the switch to the publication flow is not documented as intended.
- `apps/dashboard/src/app/admin/agent/[agentId]/page.tsx:420-424` async function applyActive(next: boolean) {
    if (!activeTenantId || !agentId) return;
    if (next) { setToast(tDraft('activationReview')); return; }
- `apps/dashboard/src/app/admin/agent/[agentId]/page.tsx:763` onClick={() => isActive ? setConfirmActive(false) : setToast(tDraft('activationReview'))}
- `apps/dashboard/messages/es.json:346` "activationReview": "La reactivación requiere revisar y publicar una versión.",
- `apps/api/src/modules/persona/persona.controller.ts:809-810` if (body.isActive !== false || Object.keys(body).some(key => !['isActive', 'expectedVersion'].includes(key)))
            throw new BadRequestException({ error: 'agent_draft_contract_required' });

### W2-04 — CONFIRMED_WITH_CORRECTION (severity: keep)
The three copy strings exist at HEAD (es.json:6699, 6718) and were introduced by cb9088cb (2026-09-13, "lead saved drafts into reviewed publication"), so steering users toward publication is intentional, but the copy's causal claim is not enforced anywhere. Runtime resolution (`resolvePersonaForChannel` → `readServingPersona`) serves ANY `agent_personas` row with `is_active=true` that is bound to the channel or `is_default=true`; there is no publication/candidate check. The onboarding bootstrap (`createDefaultAgentFromGoals`) inserts the template agent with `is_active=true, is_default=true` and `channels` = all five types, so as soon as WhatsApp is connected the template agent replies. Publication (`agent-publication-store.ts:121`) merely UPDATEs that same operational row. Correction: the cited persona.service.ts:3090-3094 lines are template-lookup code, not runtime gating evidence; the real evidence is serving-persona.ts:14-18 and persona.service.ts:3136-3142.
- CORRECTION: Evidence lines should be apps/api/src/modules/persona/serving-persona.ts:14-18 (no release filter, only is_active/channels/is_default) and persona.service.ts:3136-3142 (bootstrap agent created active on all channels). persona.service.ts:3090-3094 is template resolution, unrelated to gating.
- BY DESIGN: Copy added in cb9088cb "fix(onboarding): lead saved drafts into reviewed publication" — the owner intends the wizard to funnel users through publication, but no doc or code asserts the runtime is gated on it.
- `apps/dashboard/messages/es.json:6699, 6718` "pageSubtitle": "Tres pasos para preparar y probar; publicarás la versión revisada al final." ... "subtitle": "Conecta el canal que recibirá mensajes. El agente empieza a responder después de asignar 
- `apps/api/src/modules/persona/serving-persona.ts:14-18` FROM agent_personas WHERE is_active=true AND (($1::text IS NOT NULL AND $1=ANY(channel_bindings)) OR $2=ANY(channels) OR is_default=true)
- `apps/api/src/modules/persona/persona.service.ts:3136-3142` INSERT INTO "${schemaName}".agent_personas (name, template_id, config_json, is_active, is_default, channels, schedule_mode, created_by) VALUES ($1, $2, $3::jsonb, true, true, $4::text[], '24_7', $5), 
- `apps/api/src/modules/persona/persona.service.ts:797-800` return readServingPersona((sql,params)=>this.prisma.executeInTenantSchema(schemaName,sql,params),channelType,accountId);

### H4-09 — CONFIRMED_WITH_CORRECTION (severity: keep)
All three citations exist at HEAD as quoted. The signup funnel (tenants.service.ts:2022-2064) aggregates only four tenant timestamps (onboardingCompletedAt, firstChannelConnectedAt, firstMessageAt, subscriptionStatus) plus median TTFC/TTFM; financials.service.ts:228-282 defines activation as ">=1 active channel". `onboardingStage` is written from auth/persona/channel-management and read back per user (auth.controller.ts:217) but no SQL or service groups or counts it; no onboarding_step/tour/checklist-item events exist in apps/api/src. Correction: the platform does have a coarse time metric (median hours signup→first channel and signup→first customer message, lines 2043-2054), so "no time-to-value" is slightly overstated; what is missing is per-step, per-item, per-tour and time-to-first-*test*-reply. Not deliberate: docs/onboarding-redesign-2026-q2.md:370-371 lists "Drop-off por paso" and "Drop-off en el popup de Meta" as desired KPIs, still unimplemented.
- CORRECTION: The funnel already computes median hours signup→first channel and signup→first customer message (tenants.service.ts:2043-2054), so time-to-value is not entirely absent; the gap is per-step/per-item/per-tour drop-off and time-to-first-test-reply, and onboardingStage is never aggregated (only read per user).
- BY DESIGN: Not deliberate: docs/onboarding-redesign-2026-q2.md:370-371 lists per-step and Meta-popup drop-off as target KPIs that were never implemented.
- `apps/api/src/modules/tenants/tenants.service.ts:2022-2028, 2043-2064` const onboardingDone = signups.filter((s: any) => s.tenant?.onboardingCompletedAt).length; ... // TTFV (time-to-first-value): horas signup → primer canal conectado. ... { key: 'onboarded', label: 'Onb
- `apps/api/src/modules/financials/financials.service.ts:228-230` Onboarding activation & time-to-first-value (TTFV) ... "activated" once it has >=1 active channel. TTFV = first active channel
- `apps/api/src/modules/tenants/tenant-stall-diagnosis.util.ts:69-86` findings.push({ code: 'channel_never_connected' ... 'no_agent' ... 'onboarding_incomplete'
- `apps/api/src/modules/auth/auth.controller.ts:217-218` const onboardingStage = await this.authService.resolveOnboardingStageForTenant(user?.tenantId); return { success: true, data: { ...user, onboardingStage } };

### H7-01 — CONFIRMED_WITH_CORRECTION (severity: lower)
Tip text exists verbatim at es.json:9271. Page order at HEAD: HelpPanel (L700, collapsed by default) → hero with the channel-assignment block (L781, anchor "agent-channels") → TabNav (L906) → tab contents (Identidad/Instrucciones/etc.). So relative to the help panel the assignments are indeed below, but relative to tips 1–5 (which send the user into the tabs, further down) "Arriba" is directionally correct. The tip is ambiguous rather than flatly wrong. Two factual corrections: (1) 6f02b278 (Sep 13) only reworded tip 6 — the "Arriba" wording was introduced by 4f2390e4 on Sep 4 ("6. Arriba, marca los canales que este agente atiende y déjalo Activo"); (2) the Sep 13 change deliberately shifted the tip to the draft→test→review→approve→publish flow (commit "separate connection from agent publication"), so the process wording is intentional, not drift. Video-observed confusion is unverifiable from code.
- CORRECTION: "Arriba" dates from 4f2390e4 (2026-09-04), not 6f02b278; the Sep 13 commit rewrote the tip to describe the publication flow. The direction is correct relative to the tabs (tips 1–5) but wrong relative to the collapsed HelpPanel at the top; the defect is ambiguous framing, not an inverted arrow. Severity: medium.
- BY DESIGN: Commit 6f02b278 "docs(onboarding): separate connection from agent publication" intentionally rewrote tip 6 (and editor description, setup-wizard description, testAgentDesc) to stop implying an assigned channel answers before publication. The wording of the publication steps is a deliberate owner decision; the "Arriba" spatial hint was inherited from 4f2390e4, not re-evaluated.
- `apps/dashboard/messages/es.json:9271` "6. Arriba, prepara las asignaciones de canal y el estado que quieres publicar. Guarda el borrador, pruébalo, prepara la revisión, apruébala y publícala; sólo entonces esas asignaciones atienden clien
- `apps/dashboard/src/app/admin/agent/[agentId]/page.tsx:700-703` <HelpPanel title={th("agentEditor.title")} … tips={th.raw("agentEditor.tips") as string[]}
- `apps/dashboard/src/app/admin/agent/[agentId]/page.tsx:781-783` {/* Channel assignment — inline. Only CONNECTED accounts are offered. */} … id={guidedTourAnchorId("agent-channels")}
- `apps/dashboard/src/app/admin/agent/[agentId]/page.tsx:906-914` <TabNav … {activeTab === "persona" && (

### L5-03 — CONFIRMED_WITH_CORRECTION (severity: lower)
Evidence holds at HEAD: the wizard's step 1 (Meta wall) offers only WhatsAppConnectPanel, SecondaryChannels, "Conectar después" and "Mostrarme cómo"; the deferral copy (laterHint/subtitleDeferred) only promises a reminder in Inicio; the done step's essentials are channel/knowledge/team; Home (`admin/page.tsx`) only links back to the wizard; `InitialSetupCard`/`SetupBanner`/`AgentReadinessBanner` never reference `/admin/agent/simulation`; the WhatsApp channel page's test block is gated on `isConnected && phoneNumber`. Correction: a Meta-free test chat IS in the same flow — `AgentTestChat` is rendered on step 0 (agent step, lines 608-616), one "Anterior" click before the wall — so the claim "never offered" is too strong; it is not re-offered at the wall or after deferral. Also the WA page lines are 355-369, not 353-371. "48 minutes, zero answers" is a narrative estimate, not code-verifiable. No doc/comment shows this omission was deliberate.
- CORRECTION: The test chat is offered on wizard step 0 (AgentTestChat, setup-wizard/page.tsx:608-616) immediately before the connect step; what is missing is any pointer to it (or to /admin/agent/simulation / web widget) at the Meta wall (lines 684-702), in the deferred copy (es.json:6720, 6726), on the done step (711-731), on Home, or on the WhatsApp channel page when not connected (355-369, not 353-371). Shareability claim unverified.
- `apps/dashboard/src/app/admin/setup-wizard/page.tsx:608-616` <AgentTestChat tenantId={tenantId} agentId={workspace?.agentId ?? null} ... blocked={...} />
- `apps/dashboard/src/app/admin/setup-wizard/page.tsx:684-701` {t("connectStep.laterTitle")} ... onClick={() => void connectLater()} ... {t("connect.connectLater")} ... {t("connectStep.showMe")}
- `apps/dashboard/messages/es.json:6720, 6726` "laterHint": "Puedes dejarlo para después: te lo vamos a recordar en Inicio..." / "subtitleDeferred": "Te esperamos en Inicio para conectar WhatsApp..."
- `apps/dashboard/src/app/admin/channels/whatsapp/page.tsx:355-369` id={guidedTourAnchorId("whatsapp-test")} ... href={`https://wa.me/${phoneNumber...}`}

### R6-04 — CONFIRMED_WITH_CORRECTION (severity: keep)
Substance holds at HEAD. `buildRecommendations` turns every preparation check at fail/unknown into `fix_<code>`, and a critical check at `fail`/`unknown` yields `severity: 'critical'` (lines 1178-1187). A day-0 tenant necessarily trips one: with no assigned channels `channel_assignment` (critical, line 922) fails; with channels assigned but none connected `channel_connection` (critical, lines 929-944) fails; `business_identity`/`handoff_triggers`/`human_handoff_route` are also critical. `shouldShowQualityAttentionBanner` shows the red bar whenever `topAction.severity === 'critical'` (quality-health.ts:16), and the banner only returns null on setup-wizard, quality center, tour, focused-signal, and `/admin` while onboarding guidance owns home. Corrections: the quoted severity line omits the warning branch (warning→high/medium, lines 1185-1187); `/admin` is also suppressed (line 81). Deliberate: the sep-2026 plan chose to suppress only on `/admin` (doc line 591) and logged the day-0 pile-up as defect #1 — no `onboardingStage` gate exists in the signal service.
- CORRECTION: Severity is a nested ternary: a critical check at `warning` gives 'high' (not 'critical'); only fail/unknown gives 'critical'. The banner is also suppressed on `/admin` while onboarding guidance owns home (line 81), not just wizard and quality center — so on day 0 the red bar appears on every other admin page.
- BY DESIGN: docs/assist-quality-guided-tours-plan-2026-09.md line 591 records the deliberate scope: QualityAttentionBanner returns null only on `/admin` while `onboardingStage` landing !== 'normal'; line 514 lists the day-0 pile-up as known defect #1. Suppression on other pages before go-live was never chosen.
- `apps/api/src/modules/quality/agent-quality.service.ts:1178-1187` code: `fix_${check.code}`, … severity: check.status === 'warning' ? (check.critical ? 'high' : 'medium') : check.critical ? 'critical' : 'high',
- `apps/api/src/modules/quality/agent-quality.service.ts:922, 929-944` add({ code: 'channel_assignment', … status: status(assignedCount > 0), critical: true … code: 'channel_connection' … connectedOperational === 0 || credentialAffectedAssignments > 0 ? 'fail' … critical
- `apps/dashboard/src/components/quality/QualityAttentionBanner.tsx:79-83, 106` if (pathname === "/admin/setup-wizard" || pathname.startsWith("/admin/agent/quality")) return null; if (pathname === "/admin" && isOnboardingGuidanceOwningHome(onboardingLanding)) return null; … t(sum
- `apps/dashboard/src/lib/quality-health.ts:14-17` return summary.topAction.severity === "critical" || summary.worstStatus === "at_risk";

### L5-02 — CONFIRMED_WITH_CORRECTION (severity: lower)
Evidence holds at HEAD. `windowNeverOpened`/`giveUp` (558-562) is the only time-based exit and is a deliberate no-op once focus was lost or Meta posted anything; the button is `disabled={!sdkLoaded || busy}` (597) and there is no Cancelar control anywhere in the component, so after the popup opens the only exits are the FB.login callback (371-393) or a Meta CANCEL/ERROR postMessage (298-320). Correction: Meta-side ERROR/CANCEL events ARE surfaced immediately via postMessage without waiting for the popup to close, but Meta only posts on FINISH/CANCEL/ERROR (comment line 84), so anything non-terminal inside the popup stays silent. The video claim ("authorization" within ~12 s, no popup visible) cannot be verified from code: that key only fires when the SDK callback returned without a code, i.e. the SDK did answer. The behaviour is a deliberate owner decision in commit 4f2390e4 (comment 548-566: never reset while the person is inside Meta), which fixed the opposite bug.
- CORRECTION: Meta ERROR/CANCEL postMessage events (lines 298-320) are reported immediately, not only after the popup closes; what remains unreported are non-terminal states inside the popup, because Meta only posts on FINISH/CANCEL/ERROR. The "no popup visible in the capture" observation is unverifiable from code (screen recorders often omit popup windows) and the 'authorization' message proves the SDK callback did fire.
- BY DESIGN: Commit 4f2390e4 and the inline comment (lines 548-566) deliberately make both timers a no-op once the window opened ("Ninguno puede fallar hacia 'popup bloqueado' mientras la persona esté trabajando dentro de la ventana de Meta"), fixing an earlier bug where the 75 s backstop reset the button while users were still inside Meta. The absence of a Cancel control while launching is not documented as a decision.
- `apps/dashboard/src/app/admin/channels/whatsapp/WhatsAppEmbeddedSignup.tsx:558-570` const windowNeverOpened = () => !focusLostRef.current && !metaSignalRef.current;
    const giveUp = () => {
      if (!launchingRef.current || !windowNeverOpened()) return;
      reportFailure({ key: 
- `apps/dashboard/src/app/admin/channels/whatsapp/WhatsAppEmbeddedSignup.tsx:594-597` <button
        id={guidedTourAnchorId("whatsapp-connect")}
        onClick={launchSignup}
        disabled={!sdkLoaded || busy}
- `apps/dashboard/src/app/admin/channels/whatsapp/WhatsAppEmbeddedSignup.tsx:378-387` if (!response.authResponse?.code) {
          const terminalEventAlreadyReported = terminalEventRef.current !== null;
          const details = getEmbeddedSignupErrorDetails(response);
          if (!
- `apps/dashboard/src/app/admin/channels/whatsapp/WhatsAppEmbeddedSignup.tsx:310-315` if (embeddedEvent.event === "ERROR") {
        terminalEventRef.current = "error";
        const details = getEmbeddedSignupErrorDetails(embeddedEvent.data);
        console.error("[EmbeddedSignup] Si

### R6-03 — CONFIRMED_WITH_CORRECTION (severity: keep)
Evidence holds at HEAD. `operationalStateFromCheck` maps `warning`→`pending` (line 77); `rollUpOperationalState` returns the least-advanced known part (149); the assessment rolls up quality status + every setup task + every channel (377-380), so mission `warning`/tests `warning` (241-245, 304-316) and an `hours` task whose `business_hours` check is `warning` (setupTaskStatus 46-49) all drag the agent to `pending`, rendered as "El agente todavía no está listo para atender: queda configuración por hacer" (es.json:11995). Correction: this is deliberate, not accidental — commit 5deee1bd and the inline comment (373-376) chose it so the whole cannot read "operating" while a mission task is unproven; the mission/tests overrides were written specifically to say `pending` rather than `degraded`. Side effect worth noting: with required intents, the tests task is `tested` or `pending`, so the `prepared` rung ("configurado, no probado") is effectively unreachable and untested agents get the "configuration missing" copy.
- CORRECTION: Behaviour is as described, but it is an explicit owner design (commit 5deee1bd "four states that lied", comment at agent-assessment.service.ts:373-376 and the shared doc comment "it is the least advanced part that speaks"), so the report must frame it as a deliberate honesty trade-off, not a bug. Also: the roll-up includes quality overview status and channel states, not only tasks; and `pending` is not flagged as needs-attention (operationalStateNeedsAttention is false), so the badge is informational rather than an alert.
- BY DESIGN: Commit 5deee1bd "fix(T3): evidence that was permanently not_verified, and four states that lied with it" introduced folding tasks and tools into the roll-up; inline comment (agent-assessment.service.ts:371-376) and agent-operational-state.ts:132-141 state the intent: a whole may not read "operating"/"prepared" while any part is unproven.
- `packages/shared/src/agent-operational-state.ts:77-78` case 'warning': return options.operationalIssue ? 'degraded' : 'pending';
        case 'fail': return 'pending';
- `packages/shared/src/agent-operational-state.ts:145-149` if (known.includes('degraded')) return 'degraded';
    if (known.includes('unknown')) return 'unknown';
    return known.reduce((worst, state) => PROGRESS[state] < PROGRESS[worst] ? state : worst, kno
- `apps/api/src/modules/copilot/agent-assessment.service.ts:241-245` status: ... configured ? 'pass' : 'warning', ... configured ? undefined : 'pending')
- `apps/api/src/modules/copilot/agent-assessment.service.ts:373-380` Tools and the mission's own tasks are parts of the agent: leaving them out let the whole read "operating" while a needed tool was blocked and not one task of the mission had been proven.
 state: rollU

### H7-03 — CONFIRMED_WITH_CORRECTION (severity: lower)
Evidence exists verbatim at HEAD (i18n-parity.spec.ts:124-140): the test joins four strings (channels.whatsapp.testAgentDesc, help.agentEditor.description, help.agentEditor.tips, help.setupWizer.description) and asserts the concatenation matches a publication regex, plus does NOT match "ya está activo en este número". So the suite does positively require publish vocabulary somewhere in day-0 help. Correction: the regex runs over the JOINED copy, not per key, so simplifying help.setupWizard.description alone would NOT fail CI as long as any of the other three keeps the word (today all four contain it). Only stripping it from all four fails. Also the guard is deliberate and recent: commit 6f02b278 (2026-09-13, "docs(onboarding): separate connection from agent publication"), whose actual intent is the negative assertion (stop copy from promising the agent is live on the number); the positive match is the mechanism chosen. Current es.json copy is indeed jargon-heavy ("borrador… prepara la revisión, apruébala y publícala").
- CORRECTION: The assertion is one regex over the concatenation of four keys, not a per-key requirement. Simplifying help.setupWizard.description (or the agentEditor tips) on their own would not fail CI while testAgentDesc or agentEditor.description still mention publication; CI only fails if the publish word disappears from all four. The fix hint should be "split the assertion: keep the positive match on testAgentDesc/agentEditor.description, drop it (keep only the negative 'ya está activo' guard) for setupWizard/tips".
- BY DESIGN: Commit 6f02b278 (2026-09-13) "docs(onboarding): separate connection from agent publication" introduced this test deliberately to stop help copy from claiming a connected number is already answering before a version is published.
- `apps/dashboard/src/lib/i18n-parity.spec.ts:124-139` const copy = [ source.channels.whatsapp.testAgentDesc, source.help.agentEditor.description, source.help.agentEditor.tips.join(" "), source.help.setupWizard.description ].join("\n"); … expect(copy).toM
- `apps/dashboard/messages/es.json:help.setupWizard.description` Son tres pasos para preparar el borrador y conectar WhatsApp. Al terminar, revisa la asignación, prueba el borrador y publica la versión aprobada antes de atender clientes.

### V1-02 — CONFIRMED (severity: keep)
Code at HEAD confirms it. `createDefaultAgentFromGoals` (called from auth.service.ts:2007 and :2311 during signup) inserts the default agent with `channels = ['whatsapp','instagram','messenger','telegram','web_widget']` regardless of what is connected (persona.service.ts:3142); the same 5-type seed is repeated in the legacy migration path (:858) and in persona.controller.ts:439 for a first agent. The agent editor (`admin/agent/[agentId]/page.tsx:793-806`) renders one amber row per `assignmentIssues` entry with the raw slug as `{assignment}` and a "Quitar del borrador" button, so a day-0 tenant with nothing connected gets 5 "asignado, pero sin una conexión activa" rows. The video itself I could not inspect, but the code path produces exactly that. The seed is deliberate (controller comment: "Los cinco tipos solo se siembran cuando el tenant no tiene ningún otro agente"), predating the review-warning UI added in a7337a10/20f703d1 (Aug 2026); their interaction is not documented as intended.
- BY DESIGN: Seeding all 5 channel types on the first agent is a deliberate choice (persona.controller.ts:433-441 comment; same list at persona.service.ts:858 and :3142, present since 20f703d1 2026-08-25). docs/assist-quality-guided-tours-plan-2026-09.md §channel_coverage treats "assigned but not connected" as a non-critical fail whose fix is "quitar la asignación que no se usa, o conectar ese canal" — i.e., the warning is designed, but no doc states that a brand-new tenant should see 5 of them by default.
- `apps/api/src/modules/persona/persona.service.ts:3136-3145` INSERT INTO "${schemaName}".agent_personas (name, template_id, config_json, is_active, is_default, channels, ...) VALUES ($1, $2, $3::jsonb, true, true, $4::text[], '24_7', $5)`, ... ['whatsapp', 'ins
- `apps/api/src/modules/auth/auth.service.ts:2007, 2311` await this.personaService.createDefaultAgentFromGoals(
- `apps/api/src/modules/persona/persona.controller.ts:433-441` // Los cinco tipos solo se siembran cuando el tenant no tiene ningún otro agente ... ? ['whatsapp', 'instagram', 'messenger', 'telegram', 'web_widget']
- `apps/dashboard/messages/es.json:4728-4733` "title": "Asignaciones que necesitan revisión", ... "disconnected": "{assignment}: asignado, pero sin una conexión activa.", ... "remove": "Quitar del borrador"

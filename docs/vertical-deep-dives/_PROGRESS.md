# Deep-dives por vertical — progreso

> Un dossier por vertical, uno a la vez, siguiendo `_TEMPLATE.md`. Orden = prioridad de mercado según `market-research-latam.md` §3, con turismo adelantado por la decisión GTM pendiente. Cada dossier se commitea al completarse: si un límite de sesión corta el proceso, lo hecho queda entero y la próxima sesión retoma desde la primera fila `pendiente`.

| # | Vertical | Estado | Fecha | Notas |
|---|----------|--------|-------|-------|
| 1 | moda_belleza | completo | 2026-07-29 | INVERTIR (en belleza, no en moda): la apuesta #1 se sostiene — F1 capacidad + inactivity vivo + 3 lock-ins con bases ya en código; boutique migra a retail y la estética vive acá |
| 2 | salud | completo | 2026-07-29 | INVERTIR en dos velocidades: especialistas solistas funcionan HOY; dental (el que paga) exige F2 staff + recall vivo; Cliniko es el PMS equivocado (LatAm paga Dentalink); estética no-médica cede a belleza, salud retiene dermatología |
| 3 | inmobiliaria | completo | 2026-07-29 | INVERTIR con cirugía en el ciclo de la visita: la top-4 de demanda más barata de ganar (inventario+tools ya reales); el gap es visita sin listing + conflicto invertido + ruteo muerto + import de portales (importar, no publicar) |
| 4 | gimnasios | completo | 2026-07-29 | INVERTIR en despertar, no en construir: la reserva de clase con cupo YA existe entera (módulo gyms de mayo, corrige a la auditoría) pero es inalcanzable — nadie puede crear miembros (0 UI) y el motor de citas secuestra "quiero reservar"; nada que portar de tour_inventory, falta recurrencia semanal + waitlist + inactivity |
| 5 | turismo | completo | 2026-07-29 | INVERTIR en reparar y monetizar lo hundido: create_tour_booking está ROTO desde may-27 (INSERT 17 placeholders/15 params, quema cupos por intento) y el trial (maxProperties=0) bloquea el día 1; el GTM Hostaway NO necesita el puente cm_→properties (properties+iCal ya alcanzan) — faltan ~3-5 semanas (fix + pre-arrival + review + UI Hostaway), no los 2-3 meses del research |
| 6 | restaurantes | completo | 2026-07-29 | INVERTIR en el pedido, no en la mesa: partySize es el hueco del caso secundario (LatAm = pedidos WhatsApp); lo que corta la venta es el circuito del pedido — food_order.created sin listeners (nadie se entera), promociones enteras sin UI (patrón gimnasios) y checklist que manda el menú a la KB dejando place_order sin catálogo; SQL de restaurants.service.ts limpio (sin el patrón tours) |
| 7 | veterinaria | completo | 2026-07-29 | MANTENER + una jugada: el rubro no está rankeado en NINGÚN doc de mercado (invertir GTM sería turismo otra vez) pero el código es el mejor del clúster para el solista; el recall de vacunas se enciende casi gratis (next_due_at ya indexado, el evaluador temporal es el mismo de dental/gym/belleza); frontera cerrada: médico=vet, grooming/hotel=pet_services (label duplicado en el alta); cazados: update_pet no-op silencioso (claves snake vs camel) y create_appointment sin duration_type (la pernocta open se consulta pero no se reserva) |
| 8 | automotriz | completo | 2026-07-29 | INVERTIR en costura, no en construcción: el motor encendido ayer funciona (SQL limpio, DDL sin drift, página completa) pero 3 cables anulan el caso central — vehicleInventory=false en emprendedor/starter repite la trampa trial de turismo (403 el día 1), "prueba de manejo"/"financiación" son handoff triggers por substring que escalan la conversión ANTES del booking engine, y la UI no carga fotos (send_vehicle_image muerta); GET /search sombreado por orden de rutas → searchVehiclesForAI (bug centavos) sigue con 0 llamadores, confirma MATAR; vehicle_inquiries 0 escritores/0 lectores; test drive del chat sin vehicleId (linkage vía appointments.metadata ya viable) |
| 9 | education | completo | 2026-07-29 | INVERTIR en terminar lo desbloqueado: el circuito curso→cohorte→inscripción por chat FUNCIONA e2e post-b9bd6332 (SQL limpio, alta de cursos/cohortes con UI real), pero el dinero no cierra — payment_status queda 'pending' para siempre (createEnrollment/updateEnrollment con 0 llamadores), get_placement_test_link es callejón sin salida (0 escritores de test_url en el repo) y cancel_enrollment miente ("cupo liberado" sin devolver el asiento). Precisión: el offer_required "conservado" vive en el pipeline GENÉRICO (:511), no en las etapas de education; fusión de las 2 páginas de cursos es solo UI (misma tabla física) |
| 10 | seguros | completo | 2026-07-29 | INVERTIR en cerrar el circuito, no en ampliarlo: WTP 9/10 (máximo de la tabla) y el mejor SQL del clúster, pero cotizar→emitir→reclamar se corta en la emisión (createInsurancePolicy con 0 llamadores — patrón gimnasios en pólizas, 3 de 6 tools contra tabla impoblable) y el reclamo es teatro triple (handoff substring 'reclamo|siniestro' lo anula antes — patrón automotriz; cancel_quote muerta doble: exige status 'pending' que nadie escribe y 'cancelled' ni es válido; claim radicado sin evento ni notificación). Verificación de identidad diseñada sin pedir cédula: contact_id match (list_my_claims ya lo hace) + OTP del customer portal existente; renovación = 5º rubro esperando el cron temporal, pero acá sobre fechas de póliza (índice next_payment_at ya listo), no inactividad |
| 11 | servicios_hogar | completo | 2026-07-29 | MANTENER + cerrar el circuito de emergencia: la mejor ficha del Tier 3 (SQL limpio, modelo dispatch correcto) pero sin demanda documentada (solo PR #5) — y el caso estrella se auto-sabotea: 'emergencia'/'fuga de gas' son handoffTriggers evaluados ANTES de la IA → escala sin crear el request y el listener de 5c2581db nunca dispara en su caso nominal (hasta la FAQ sembrada '¿Atienden emergencias 24/7?' escala a humano); urgency=alta sin notificación y board sigue en refresh-20s. Contradice el MATAR de photos SI se elige la jugada única (cotización por foto: persistencia de imagen ya existe para audio, es clonar); MATAR de staff-scheduling OK (el despacho necesita directorio, no disponibilidad). Cazados: cancel reason a void doble (sin columna notes + sin map), check_request_status exige UUID que el cliente no tiene y sin ownership, activación de ops chequea services que la vertical siembra vacía a propósito |
| 12 | pet_services | completo | 2026-07-29 | MANTENER — dos verticales, un módulo: no fusionar con vet pero prestarle pets (enablePetsTool+showPets = 2 líneas; hoy Luna exige "confirma vacunas" sin tool, /admin/pets vacía para siempre y la activación de plataforma da falso negativo); la pernocta open se consulta Y se reserva post-fixes (circuito verificado) pero el motor directivo trata la ventana como UN slot (rechaza las 17:00 afirmando que 08:00-18:00 está libre); la estadía multi-día sigue sin modelo (check_daycare ignora checkOut/petSize de su schema, cuenta baños como cupos y su comment miente sobre blocked_dates) y UN perro en guardería 480min mata la agenda entera hasta las 16:00; bySubType tienda→tpl_pet_tienda muerto fuera de es (localize devuelve solo templates[0] — el pet shop brasileño recibe a la peluquera) |
| 13 | fotografia | completo | 2026-07-29 | MANTENER + 3 jugadas baratas: no es "de plantilla" (módulo real, 4 tools, SQL limpio) pero las 3 patas del rubro (portfolio/seña/galería) están 0/3 y el mercado no la respalda (0 menciones en research/competitivo). Cazados: dos sistemas de reserva que no se ven (request_photo_quote escribe photo_sessions, check_date_availability solo cuenta appointments → la sesión de la IA no ocupa la fecha) y el eje de entrega inalcanzable entero (página read-only, create/update/deliver de api.ts con 0 llamadores, photo_session.requested con 0 listeners, gallery_url imposible de setear). Diseño clave del fix pendiente del umbral: solo la sesión SEÑADA (deposit_paid>0) bloquea la fecha — la seña ES el bloqueo; boda 480min inagendable en sábado (ventana 360min) |
| 14 | retail | completo | 2026-07-29 | MANTENER-digno + cerrar el circuito del pedido; apuesta e-commerce NO este trimestre pero decidida en voz alta (mercado 10/WTP 5 + Tidio/Chatfuel 9-vs-5). El escaparate habla pero no cobra: create_order NO EXISTE (la conversión muere en "¿cómo pago?"), Shopify/Woo es backend sin UI (0 llamadores de config/sync en dashboard, api.ts sin métodos), abandoned_carts muerta en ambos extremos (writer 0 llamadores, 0 lectores, syncOrders/cartAbandonmentEnabled flags fantasma). Cazados: keywords de plataforma 'devolución/descuento' anulan postventa y ofertas antes de la IA; auto-progress mapea "¿precio?"→Pedido y "lo compro"→Enviado (prob ladder sin order_required); send_product_image muerta (UI sin campo de foto + updateProduct no-op); tools.policies apagada en el rubro de las políticas mientras postventa inventa "30 días". Boutique recibida: gana persona de ventas + catálogo real y pierde la agenda absurda, pero solo vale empaquetada con QWs + create_order |
| 15 | finanzas | completo | 2026-07-29 | GENÉRICA-HONESTA + 1 ruteo: `finanzas>seguros` debe rutear a la vertical seguros en el punto único `auth.service.ts:1562` (hoy el broker queda sin las 6 tools de insurance, sin `/admin/insurance` —sidebar gateado a `verticals:["seguros"]`— y con pipeline de crédito; encender solo la tool en el bootstrap NO alcanza: el catálogo queda impoblable). La demanda documentada de "finanzas" ES demanda de seguros (top-10 tiene Insurance Brokers #9 WTP 9; Panamá/Argentina/Brasil listan "financial services / **insurance brokers**"). Techo estructural propio: el L1 imborrable veta la asesoría de inversión y el pedido de government IDs (`prompt-assembler:94,98`) — el núcleo del sub-tipo asesoría y la captura que su plantilla ordena. Circuito agendar-asesoría VERIFICADO e2e post-`b9bd6332` (servicio 30min entra en la ventana, $0 sin pasarela). Cazados: la pre-calificación no tiene destino (0 tools de escritura sobre contacto en las 80+ del executor, aunque `custom_attribute_*` ya es riel del motor de reglas) y el auto-progress "pre-aprueba" créditos por probabilidad ("me interesa"→Pre-aprobación, "cuánto"→Documentación) sin una sola `transitionRule`. Precisión: `offer_required` nunca estuvo en finanzas (vive en el pipeline genérico `:511`) |
| 16 | servicios_profesionales | completo | 2026-07-29 | GENÉRICA-HONESTA + apagar el desactivador: **ningún sub-tipo rutea** (a diferencia de `finanzas>seguros`) y las 3 tools de `vertical-strategy.md:158-166` NO valen — `get_services_and_fees` ya existe (`list_services` con `price`: los 2 servicios sembrados llevan honorario y el motor lo muestra), `schedule_consultation` es el motor de reservas (circuito e2e verificado, mejor que finanzas), `qualify_case` es texto libre, y `request_documents` es **inconstruible** hoy. Cazados: `'abogado'` y `'demanda'` son `complaintKeywords` de PLATAFORMA (`handoff.service.ts:76`) evaluados antes de la IA con retorno temprano → el intake canónico del rubro ("necesito un abogado") escala sin que Elena hable, y el escape documentado `handoffCategories` tiene **0 escritores** en todo el repo; un PDF entrante muere en `conversations.service.ts:1383-1392` con "solo texto/imágenes/audios" (el adapter sí lo parsea, el archivo se tira); auto-progress que **abre expedientes** ("cuánto cobran"→Propuesta 50, "reservar"→En proceso 75 — el vocabulario que induce el propio motor de reservas) sin una sola `transitionRule`; "portal seguro" prometido por la plantilla de seguimiento con `customer-portal` sin UI (0 páginas) y sin documentos; inbox sin need-to-know (`filter='all'` sin filtro, `agentId` por query param) en el rubro del secreto profesional; paso 6 del borrado GDPR roto por orden (teléfono real sobrevive en `campaign_recipients`). A favor y poco común: el L1 imborrable **coincide** con la política del rubro (no es el veto de finanzas), y fuera de español se pierden `tpl_legal_seguimiento` y la regla de confidencialidad (`localizeVerticalTemplates` colapsa a `[0]`) justo en los 2 únicos mercados con demanda documentada (PR :230 / UY :339, ambos premium). Frontera `consultores` vs `consultoria_ti` heredada al dossier 17 |
| 17 | technology | completo | 2026-07-29 | GENÉRICA-HONESTA + reparar los números B2B: **no hay ruteo** (los 4 sub-tipos son un mismo motion) y **no hay tools que valgan** — la lente "nosotros SOMOS un SaaS B2B" muestra que para vendernos usaríamos casi exactamente lo que ya existe (Diego SDR, demo agendada, KB/RAG sobre docs, handoff al AE con resumen LLM, embudo B2B, organizaciones). Pero el hueco real no es de conversación sino **de números**: `forecasting.service.ts:4` excluye del pipeline abierto `('ganado','perdido','no_interesado')` y technology cierra en **`cerrado`** → todo deal ganado queda para siempre en el forecast ponderado al 100%, la velocidad es null (`won_at` solo se escribe si el slug es literalmente `'ganado'`, `opportunities.repository.ts:314`) y win-rate/leaderboard/attribution dan 0 — mismo defecto en crm-analytics y attribution, **ya resuelto en `analytics.service.ts:368`** y no propagado; afecta a las 18 verticales, pero solo esta lo mira. Cazados: el motor de reservas **secuestra "demo"** (servicio único "Demo personalizada" → match por palabra) y anula la regla estrella "agenda demo SOLO con leads calificados" — el BANT nunca corre (patrón gimnasios, pero acá calificar-antes-de-agendar ES el producto); `'no funciona'` es **complaintKeyword de PLATAFORMA** (`handoff.service.ts:76-79`, retorno temprano) → el ticket de soporte canónico escala antes de que Diego Soporte hable, y ese template **ni siquiera se activa** (el objetivo `support` del alta no selecciona nada: siempre gana `templates[0]`); fuera de `es` se pierde la plantilla de soporte Y las 6 reglas BANT (`localizeVerticalTemplates` colapsa a `[0]` y pisa `rules` con el registry) justo en los idiomas del comprador más probable; auto-progress sin una sola `transitionRule` asciende "¿cuánto cuesta?"→**Propuesta 55%** y "me interesa"→**Demo** — infla el mismo forecast que A1 arregla; `crm.deal_rotting` emitido con **0 listeners** (el dato sí es alcanzable en `/admin/contacts/organizations`, pero nadie avisa). crm-b2b **no es huérfano**: es horizontal, con UI viva y los 3 endpoints consumidos. QWs confirmados: 6 de 8 handoffTriggers no pueden disparar (substring; `integracion compleja` ni matchea por tilde), `labelOverrides:{}` + `hiddenItems` **no-op doble** (inventory/orders ya allow-listeados a retail/restaurantes, `catalog` no existe como labelKey), FAQs de "devoluciones/ubicación" en un SaaS remoto, `ToolsTour.technology:[]` en la única vertical con agenda cuyo tour no la muestra, y `transactionNoun:'deal'` que el L1 ordena decirle **al cliente**. Mercado: 0 menciones en `vertical-strategy.md` y fuera del top-10; lo que los docs sí piden es vender **a través** de este rubro (Reseller/SaaS Mode 2/10, "el motor de crecimiento viral con agencias LatAm") — canal, no vertical |
| 18 | otro | completo | 2026-07-29 | MANTENER — pero arreglar el piso, que no es opcional: `otro` no es una vertical, es el contrato mínimo de la plataforma, y sus 3 defectos centrales golpean a las 18. **El hallazgo**: `otro` es la ÚNICA vertical que hereda el pipeline genérico con sus `transitionRules` (las otras 4 `createGenericVertical` sobrescriben `pipeline` y quedan con embudo sin gobierno), y esa herencia trae `offer_required` en la etapa `propuesta` — una regla que hace `JOIN leads.course_id = commercial_offers.course_id`, columna que solo education escribe. Bloqueo triple verificado: la página de ofertas existe (`/admin/catalog/offers`) pero **no tiene entrada de sidebar en ninguna vertical** (único enlace: el ToolsTour de retail), su formulario **no expone `courseId`**, y con ambos lados en NULL el JOIN igual no matchea → columna Propuesta inalcanzable a mano Y auto-progress congelado en Calificado para siempre (el mapeo por probabilidad manda "lo compro" 95 y "me interesa" 80 a `propuesta`=60, la única no-terminal por encima). Cazados además: `tpl_otro_soporte` con **0 caminos de selección** (la rama por goals se saltea porque `getVerticalTemplates('otro')` es truthy — el que pide soporte recibe ventas, patrón technology); la plantilla ordena "agenda una reunión" con `bookingEnabled:false` + `services:[]` garantizando que la tool quede OFF; `inventory`/`orders` allow-listeados a retail/restaurantes → la ferretería no tiene dónde cargar productos aunque `CATALOG_TOOLS` esté entera; **la FAQ semilla "¿Tienen política de devolución?" la bloquea `'devolucion'` como complaintKeyword de PLATAFORMA** (la plataforma preparó la respuesta y después prohibió la pregunta), igual que `'descuento'` mata el caso mayorista; 2 de 3 `handoffTriggers` propios sombreados por platform (`queja`, `reembolso`) y el tercero (`emergencia`) por substring desnudo; el modal de super_admin ofrece **4 slugs que no existen en el registro** (`restaurante`,`ecommerce`,`servicios`,`educacion`) → tenants "Restaurante" que son `otro` en silencio, y `TenantsService.create()` **no llama a `bootstrapVertical`** (1 solo llamador en el repo). Doble slug `other`/`otro` CONFIRMADO pero acotado: nace en `auth.service.ts:1603`, sobrevive verbatim en `verticalConfig.industry` (`verticals.service.ts:212`) y produce dos claves de fallback distintas en las dos vistas de plataforma (`'otro'` en vertical-analytics vs `'other'` en el dashboard super_admin); las etiquetas no explotan gracias a los guards `.has()`. Fix `5c2581db` VERIFICADO e2e (productor `conversations.service.ts:1622-1648` desprefija `other:`, consumidor `prompt-assembler:164-165`, regla L1 #13) con dos precisiones nuevas: las opciones tildadas bajan como claves de enum crudas (`lead_qualification`, `b2c`) y el dato es **irrepetible** (0 referencias en el dashboard — no hay dónde corregirse). Mercado: recorriendo los ~85 nichos país-por-país de `market-research-latam.md` §2, solo **2 no tienen vertical** (logística/freight Panamá :194, autoescuelas Honduras :141) → `otro` no es un segmento desatendido, es el residuo; no hay caso para invertir en él como vertical, sí para instrumentarlo (§7-C: "vertical emergente" leyendo el texto libre, hoy sin ninguna superficie analizable) |
